# CLAUDE.md — HS코드 분류 검증 보조 시스템

**마감: 2026-10-13** (착수 2026-08-18 + 8주)

## 이 프로젝트가 존재하는 이유

목표는 정확도 90%짜리 제품이 아니라 **"문제를 정의하고, 측정하고, 개선을 증명한 사람"이라는 증거** 하나를 만드는 것이다.

- 완벽한 미완성보다 **불완전한 배포**가 낫다

작업 중 판단이 필요할 때 기준은 하나다: **"이게 10월 13일에 보여줄 URL과 숫자에 기여하는가?"** 아니면 하지 않는다.

## 개발자 정보 (중요)

- Java/Spring/JSP 기반 SI 개발 4년차. **Python은 입문 단계**
- 주당 투입 가능 시간 10~20시간
- 코드를 받아쓰는 게 아니라 **이해하는 것**이 이 프로젝트의 목적 중 하나다

따라서 코드를 작성할 때:

- **한 번에 파일 하나, 기능 하나씩.** 여러 파일을 한꺼번에 쏟아내지 말 것
- 새 라이브러리나 Python 고유 문법(list comprehension, decorator, context manager, generator 등)이 처음 등장하면 **왜 그렇게 쓰는지 한 줄 설명**을 붙일 것
- Java와 다른 지점(타입 선언 없음, 들여쓰기 블록, `__init__.py`, 가상환경 등)은 짧게 짚어줄 것
- 에러가 나면 즉시 수정 코드를 주지 말고 **에러 메시지 읽는 법부터** 안내할 것
- 추상화·디자인 패턴을 미리 도입하지 말 것. 동작하는 단순한 코드가 우선

## 기술 스택 — 이것만 쓴다

### 금지 (요청받아도 제안하지 말 것)

- **LangChain / LlamaIndex** — 추상화가 두꺼워 디버깅이 불가능해지고, 학습 목적에 반한다
- **벡터DB (Chroma, Pinecone, FAISS 등)** — 결정례 수백 건 규모에서는 numpy 배열 + 코사인 유사도로 충분하다
- **FastAPI / Django / React** — UI는 Streamlit 기본 컴포넌트만
- **Docker / CI/CD** — 배포는 Streamlit Community Cloud
- **클래스 상속 구조, 플러그인 아키텍처, 설정 추상화** — 함수와 dict로 충분하다

## 시스템 구조

```
입력: 물품설명
  │
  ├─[0] 게이트 — 정보가 충분한가? 부족하면 되묻고 종료   (마지막에 구현)
  ├─[1] 후보 생성 — LLM에 6자리 후보 3개 요청
  ├─[2] 결정례 검색 — 물품설명 임베딩으로 유사 결정례 top-5
  ├─[3] 재정렬 — 후보 + 결정례를 함께 주고 순위 재판단   ★ 1순위
  ├─[4] 세번 확정 — 확정 6자리의 10자리 목록에서 선택     ★ 2순위
  └─ 출력: 코드 + 근거 결정례 + 확신도 + 사람이 확인할 포인트
```

설계 근거는 baseline에 있다. **모델은 이미 상당수의 정답을 후보에 담고 있다(Top3 86.2%). 새로 생성할 필요 없이 순위만 고치면 된다.** 그래서 [3]이 1순위다.

단, [3]의 사정거리는 정확히 정해져 있다:

| 구분 | 건수 (유효 29건 중) |
|---|---|
| Top1 정답 | 13 |
| **Top1 오답이지만 Top3에 정답 있음 → [3] 사정권** | **12** |
| Top3에도 정답 없음 → [3]으로 회수 불가 | 4 |
| 6자리는 맞고 10자리만 오답 → [4]의 영역 | 9 |

**[3] 단독으로 도달 가능한 이론 상한은 86.2%다.** 그 위는 [1]후보 생성을 고쳐야 한다.

## Baseline (2026-08 측정 완료, 절대 수정 금지)

`data/baseline_측정표.xlsx`에 원본이 있다. 개선 여부는 항상 이 수치와 비교한다.

| 지표 | 값 |
|---|---|
| 조건 A 유효 건수 | 29 |
| 조건 A 4자리 | 55.2% (16/29) |
| **조건 A 6자리** | **44.8% (13/29)** |
| 조건 A 10자리 | **13.8% (4/29)** |
| P2 Top1 6자리 | 44.8% |
| **P2 Top3 포함** | **86.2% (25/29)** |
| 조건 B 유효 건수 | 28 |
| 조건 B 4자리 / 6자리 / 10자리 | 53.6% / 42.9% / 21.4% |
| 품명명확 / 품번only (조건 B 6자리) | 52.4% (11/21) / 14.3% (1/7) |
| 정보부족 인지율 | 3.6% (28건 중 1건) |
| 되묻기 질문 평균 | 0.07개 |
| 일관성 6자리 / 10자리 | 90% / 60% (10건) |
| 정답 재현 (3회 중) | 6자리 1.9 / 10자리 1.3 |
| 결정례 인용 발생 | 0건 (검색 누수 없음) |

> **2026-08-20 정정** — 위 표는 엑셀의 O/X 플래그가 아니라 후보 코드에서 다시 계산한 값이다.
> `측정기록` 시트 29행의 정답 셀에 수식 문자열이 남아 있어 그 행의 플래그 5개가 전부 0으로
> 굳어 있었다. 원본 xlsx 는 고치지 않았다. 경위는 EXPERIMENTS.md 정정 항목에 있다.

**측정 조건**: `gemini-3.1-pro-preview`, Google AI Studio Playground, Grounding OFF, Temperature 1, Thinking High, System instructions 비움, 이미지 없음, 건별 새 세션, 첫 응답만 기록

**오답 유형 분포 — 분모는 10자리 오답이다 (태그 24건)**

| 유형 | 건수 | 비중 | 어느 단계가 고치는가 |
|---|---|---|---|
| 호 경합 판단 오류 | 10 | 41.7% | **[3] 재정렬** (10건 중 9건이 6자리부터 오답, 그중 7건이 사정권) |
| 한국 고유 세번(10자리) 오류 | 6 | 25.0% | **[4] 세번 확정** (6건 중 5건은 6자리를 이미 맞힌 건 — [3]으로는 손댈 수 없다) |
| 세트물품 통칙 / 용도·기능 오인 / 물품설명 정보부족 | 각 2 | 각 8.3% | — |
| 존재하지 않는 코드 생성 / 복합재질 판단 실패 | 각 1 | 각 4.2% | — |

→ 상위 2유형이 66.7%지만 **하나의 단계로 둘 다 잡을 수 없다.** [3]이 겨냥하는 건 41.7%p, [4]가 겨냥하는 건 25.0%p다.

**조건 A vs 조건 B**: 동시 유효 28건을 짝지어 보면 A만 맞은 건 3건, B만 맞은 건 4건이다. **상세 물품설명과 품명 원문 사이에 유의미한 성능 차이는 관측되지 않았다.** 즉 "정보를 더 주면 정확해진다"는 가정은 이 데이터로 지지되지 않는다. 따라서 되묻기 기능([0])의 목적은 정확도 향상이 아니라 **안전성** — 정보가 부족한데도 확신에 차서 답하지 않게 하는 것 — 으로 정의한다.

**오답의 품목 분포**: 유효 29건은 19개 류에 흩어져 있다(39류 6건, 84류 5건, 30류 2건, 나머지 16개 류 각 1건). 오답이 특정 계열에 몰려 있다는 근거는 없다.

**알려진 한계 (문서에 반드시 명시)**: 평가 셋이 공표된 결정례이므로 검색 가능 모델에서는 과대추정된다. 검색을 차단해 통제했으며(결정례 인용 0건 확인), 실제 타겟인 미결정 신규 품목 상황의 대리 지표로 사용한다.

## 목표 수치

| 지표 | baseline | 목표 | 이게 무슨 뜻인가 |
|---|---|---|---|
| **Top1 6자리** | 44.8% (13건) | **65% (19건)** | 사정권 12건 중 **6건(50%)을 뒤집어야** 한다 |
| 10자리 | 13.8% (4건) | 40% (12건) | 6→10자리 전환율을 **31% → 63%** 로 올려야 한다 |
| 일관성 10자리 | 60% | 90% | 10건 중 6 → 9건 |
| **품번only 되묻기율** | 14.3% 수준 | 70% | 안전성 지표. 아래 채점 규칙 참조 |

**목표를 넘으면 다음 단계로 간다. 더 올리려 하지 않는다.**

일관성은 구조화 출력(JSON)을 먼저 적용하고, Temperature 하향은 **[3]재정렬 효과 측정이 끝난 뒤에** 별도로 적용한다. 온도를 먼저 바꾸면 개선이 재정렬 덕분인지 온도 덕분인지 구분할 수 없다.

## 평가 셋 형식

`data/baseline_측정표.xlsx`의 `케이스목록` 시트, 5~34행:

| 열 | 내용 |
|---|---|
| A | 번호 |
| B | 결정례 번호 |
| C | 품명 원문 (조건 B 입력) |
| D | **정답 HSK** |
| E | **물품설명** (조건 A 입력) |
| F | 물품설명 출처 (원문/재작성) |
| G | 품명유형 (품명명확/품번only) |
| H, I | 유효_A, 유효_B — **X인 행은 집계에서 제외** |
| J | 제외 사유 |

`측정기록` 시트(3행 헤더, 4행 예시, 5행부터 데이터)에 조건 A 결과, `조건B_품명only` 시트에 조건 B 결과, `일관성` 시트에 3회 반복 결과, `측정조건` 시트에 환경 기록이 있다.

정답 HSK는 행마다 `3820002000` 과 `3207.20-9000` 표기가 섞여 있다. 반드시 정규화 후 비교할 것.

## 채점 규칙

- 코드 비교 전 `.`, `-`, 공백, non-breaking space(`\xa0`), 탭을 모두 제거
- 4자리 / 6자리 / 10자리를 각각 따로 집계
- Top3 포함 판정은 **6자리 기준**
- 일관성은 6자리와 10자리를 **둘 다** 집계 (10자리가 실무 기준)
- **유효 플래그가 X인 행은 반드시 제외** (조건 A와 조건 B의 유효 행이 다르다 — 29 vs 28)
- **안전성(되묻기율)**: 품번only 7건에서 되물으면 +, 품명명확 21건에서 되물으면 −. 양방향으로 집계한다
- **건별 O/X를 baseline과 대조한 diff를 항상 함께 출력한다.** 비율만 맞고 맞힌 건이 다를 수 있다

## 작업 순서

| 주 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 환경 구성 + Gemini API 호출 + **무료 티어 한도 실측** | 물품설명 1건 → 코드 응답. 29건 배치 1회 소요 시간 확인. **pro 250 req/day 실측 완료(8/20)** |
| **2** | **평가 자동화 (`evaluate.py`) + 구조화 출력(JSON)** | **29건 건별 O/X가 baseline과 일치** (비율이 아니라 diff) |
| 3 | 결정례 수집 + 임베딩 + **테스트셋 분리** | 유사 검색이 그럴듯한 사례 반환. 테스트셋은 코퍼스에서 제외 |
| 4 | 재정렬 구현 | Top1 측정, baseline과 비교 |
| 5 | 재정렬 개선 | **Top1 60% 돌파 — 테스트셋으로 판정 (1회 열람)** |
| 6 | 세번 확정 단계 | **10자리 30% 돌파 — 테스트셋으로 판정 (1회 열람)** |
| 7 | Streamlit UI + 배포 | **URL 존재** |
| 8 | 지인 피드백 + 되묻기 | 실사용자 3명 |

**2주차가 관문이다.** 평가 자동화가 되면 "고치고 → 돌리고 → 숫자 확인"이 5분 사이클이 된다. 안 되면 개선이 멈춘다. 3주차 이후 작업은 반드시 2주차 완료 후에 시작한다.

**5주차 목표 미달 시**: 목표를 낮춰 기록하고 다음 단계로 간다. "왜 65%가 안 됐는가"의 분석도 산출물이다. 일정을 연장하지 않는다.

## 홀드아웃 규칙

- 테스트셋은 3주차 수집 때 함께 확보하고, **코퍼스에서는 반드시 제외**한다
- 테스트셋 열람은 **5주차 종료 시 1회, 6주차 종료 시 1회, 총 2회뿐**
- 열람할 때마다 그 시점의 개발셋 숫자와 함께 `results/`에 기록한다
- 검색 코퍼스는 **평가셋 결정례 포함 / 제외 두 조건 모두 측정**하고 그 격차를 함께 보고한다. 포함 조건의 숫자를 단독으로 인용하지 않는다

## 실험 기록

성능에 영향을 주는 변경을 하면 `results/{타임스탬프}/`에 응답 원문·프롬프트 전문·집계 CSV를 남기고, `EXPERIMENTS.md`에 한 줄 요약을 추가한다.

```
날짜 / 변경 내용 / Top1 6자리 / 10자리 / 이전 대비
```

`results/`는 용량과 원문 때문에 gitignore하지만 **`EXPERIMENTS.md`는 커밋한다.** 이력이 곧 포트폴리오 서사다.

## 스코프 통제

다음은 요청받아도 만류하고 이유를 설명한다.

- 결정례 추가 수집 (3주차에 정한 건수에서 끊는다)
- 목표 수치 달성 후의 추가 튜닝
- UI 디자인 개선 (Streamlit 기본 컴포넌트만)
- 사용자 인증, DB, 다국어, 이미지 입력
- 아키텍처 리팩터링, 테스트 커버리지 확대
- **평가 셋 재측정** — baseline은 확정값이다

## 보안

- API 키는 `.env`에만. 코드·커밋·로그·스크린샷 어디에도 노출 금지
- `.gitignore`에 `.env`, `results/`, `__pycache__/` 포함
- 배포 앱은 **세션·일일 호출 횟수 상한**을 둔다. 공개 URL + 인증 없음 + 내 API 키이므로 상한이 유일한 방어선이다
- API는 무료 티어 안에서 운용한다.
  **실측(2026-08-20): `gemini-3.1-pro-preview` 일일 한도는 `GenerateRequestsPerDayPerProjectPerModel`
  = 250 requests/day, 리셋 약 20.7시간. 크레딧을 충전해도 풀리지 않는다.**
  토큰이 아니라 **요청 수 기준**이므로, 프롬프트를 줄여도 하루 처리량은 늘지 않는다.
  개발용 `gemini-3.7-flash`는 훨씬 넉넉하다 — 조건 간 상대 비교(ablation)는 flash로 하고,
  인용할 절대 숫자만 pro로 잰다
- 결정례는 공개 자료지만, 지인에게 받은 실무 데이터가 있다면 저장소에 올리지 않는다

## 커밋

- 한글 커밋 메시지, 한 커밋에 한 가지 변경
- 성능이 바뀐 커밋에는 수치를 적는다 — 예: `재정렬 단계 추가 (Top1 44.8% → 55.2%)`
