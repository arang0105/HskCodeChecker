# 실험 이력

성능에 영향을 주는 변경을 하면 한 줄씩 추가한다.
응답 원문·프롬프트 전문·집계 CSV는 `results/{타임스탬프}/`에 남긴다(gitignore).

| 날짜 | 변경 내용 | Top1 6자리 | 10자리 | 이전 대비 |
|---|---|---|---|---|
| 2026-08-18 | baseline (Playground 수기 측정, 유효 29건) | 44.8% | 13.8% | — |
| 2026-08-18 | 평가 자동화 + JSON 구조화 출력 (pro, 후보 3개만) | 51.7% | 미측정 | 회수 2 / 손실 0 |
| 2026-08-19 | **[3] 재정렬 추가** (코퍼스 5,921 / top_k=5 / 안 A) | **72.4%** | 미측정 | 같은 실행 내 55.2% → +5건 |
| 2026-08-19 | **[4] 세번 확정 추가** — flash 예비 측정 (pro 아님) | 72.4% | **65.5%** | 6→10 전환율 31% → 90.5% |
| 2026-08-19 | **[4] pro 공식 측정** — 6주차 목표 달성 | **75.9%** | **69.0%** | 회수 9 / 손실 0 · 전환율 90.9% |
| 2026-08-19 | **홀드아웃 테스트셋 30건 (1회차 열람)** | **56.7%** | **50.0%** | 개발셋 대비 -19.2%p / -19.0%p |
| 2026-08-20 | baseline 정정 (측정표 셀 오류 1건) | 41.4% → **44.8%** | 13.8% | 기준선만 이동 |
| 2026-08-20 | **회귀 세트 20건 신설** (쉬운 건, 강등 감시) | 85.0% | 80.0% | **강등 1 / 승격 1** |
| 2026-08-20 | 유사도 임계값 검토 | — | — | **기각** (LLM 비용 0) |
| 2026-08-20 | **[0] 되묻기 게이트 신설** | — | — | 품번only 되묻기 14.3% → **57.1%** |
| 2026-08-20 | 결정사유 ablation (flash, 개발셋 29건) | 72.4% → 69.0% | 65.5% → 62.1% | **유지 확정** |
| 2026-08-20 | top_k 5 → 20 검토 | — | — | **기각** (LLM 비용 0) |
| 2026-08-20 | 온도 1.0 → 0.2 (pro, 개발셋 29건) | 75.9% → 72.4% | 69.0% → 62.1% | **기각** |
| 2026-08-20 | **홀드아웃 30건 (2회차 = 최종 열람)** | **56.7%** | **50.0%** | **1회차와 건별 전건 일치** |
| 2026-08-25 | Streamlit → FastAPI + React 이전 | **변동 없음** | **변동 없음** | 파이프라인 무수정 (껍데기만 교체) |

**인용할 숫자는 테스트셋 쪽이다.** 위 개발셋 숫자는 개선 과정을 보여줄 때만 쓴다.

## 2026-08-18 — 2주차 관문: 평가 자동화

`results/20260818_141358/` · `gemini-3.1-pro-preview` · temperature 1 · 조건 A 29건

**건별 diff (Top1 6자리)**

| 구분 | 건수 | 번호 |
|---|---|---|
| 유지_O | 13 | 3, 6, 8, 9, 10, 11, 13, 19, 20, 22, 23, 28, 29 |
| 회수 | 2 | 26, 30 |
| **손실** | **0** | — |
| 유지_X | 14 | 2, 4, 5, 7, 12, 14, 15, 16, 17, 18, 21, 24, 25, 27 |

baseline이 맞힌 13건을 **하나도 잃지 않고** 그대로 재현했다. 채점 자동화가
baseline과 같은 대상을 측정하고 있음이 확인됐다.

**Top3 포함이 86.2% → 89.7%(26/29)로 올랐다.** [3]재정렬의 이론 상한이
그만큼 올라갔고, 사정권(Top1 오답 + Top3 정답)은 12건 → 11건이다.

- 실행 시간 704초 (24.3초/건)
- 파싱 실패 0건
- 참고: 첫 실행의 토큰 집계는 thinking 토큰을 누락했다(`candidates_token_count`에
  포함되지 않음). 이후 실행부터 `billed_out`으로 기록한다.

**주의** — 회수 3건은 프롬프트 변경 효과와 temperature 1 변동을 구분할 수 없다.
baseline 일관성이 6자리 90%이므로 3건은 노이즈 범위 안이다. 개선으로 계산하지 않는다.

## 2026-08-18 — 3주차: 결정례 수집 + 홀드아웃 분리

출처: 관세법령정보포털(CLIP) 품목분류 국내사례. 요청 구조는 `_grill/clip_요청구조.md`.

**수집 범위** — 2022-01-01 ~ 2026-07-31, 협의회 + 위원회 두 계열

2022년으로 끊은 이유는 HS 협약이 2022년에 개정됐기 때문이다. 그 이전 결정례의 세번은
현행과 다를 수 있고, 그런 사례를 근거로 제시하면 오히려 틀린 답을 밀게 된다.
일반 사전심사(품목분류사례, 수천 건)는 평가셋과 성격이 달라 제외했다.

| 단계 | 건수 |
|---|---|
| 수집 원본 | 519 (협의회 133 / 위원회 386), 실패 0 |
| 세번 추출 실패·물품설명 없음 제외 | -8 |
| 테스트셋 분리 (협의회, 류별 균등) | -30 |
| **검색 코퍼스** | **481** (그중 평가셋 29건은 플래그 Y) |
| **기본 검색 대상** | **452** |

**평가셋은 협의회결정사항이었다.** 29건 전부가 협의회에서 매칭됐다(참조번호 + 계열 +
결정세번 세 조건 일치, 실패 0). 협의회는 분류가 갈려 논의에 올라간 건들이라,
baseline 정확도 44.8%가 낮았던 것과 앞뒤가 맞는다.

**누수 통제** — 평가셋 29건을 코퍼스에서 지우지 않고 `평가셋포함` 플래그로 표시했다.
CLAUDE.md 홀드아웃 규칙이 "포함 / 제외 두 조건 모두 측정"을 요구하기 때문이다.
검색의 기본 동작은 제외이며, 포함 조건은 명시적으로 켤 때만 쓴다.

교집합 검증: 코퍼스 ∩ 테스트셋 = 0건, 평가셋 ∩ 테스트셋 = 0건.

**데이터 품질**: 물품설명 중앙값 226자(최대 3,137), 결정사유 중앙값 791자(최대 2,505),
59개 류에 분포. 목록 API는 176자에서 절단되므로 건별 상세 호출로 원문을 받았다.

### 코퍼스 확대 — 판단을 데이터로 뒤집다

452건으로 검색을 측정했더니 **top-5 회수가 27.6%**에 그쳤다. 원인을 갈라보니
정답 6자리가 **코퍼스에 아예 없는 경우가 17/29(58.6%)**였고, 있는 것 중에서는 66.7%를
찾아냈다. top_k를 50까지 늘려도 31%에서 멈춘 것이 결정적 증거였다 —
**검색 품질이 아니라 커버리지가 병목**이었다.

"품목분류사례는 성격이 달라 제외한다"는 3주차 초의 판단은 이 데이터로 지지되지 않았다.
협의회 건은 **선례가 없어서 협의회에 올라간 것**이므로 협의회끼리 모아두면 서로를 찾지 못한다.
필요한 선례는 일반 사전심사 쪽에 쌓여 있었다. 판단을 뒤집고 품목분류사례를 추가 수집했다.

| 지표 | 452건 | 3,247건 |
|---|---|---|
| 정답 6자리가 코퍼스에 존재 | 41.4% | **82.8%** |
| 검색 top-5 회수 | 27.6% | **48.3%** |
| 존재하는 것 중 회수율 | 66.7% | 58.3% |
| 재정렬 사정권(12건) 교집합 | 2건 | **5건** |
| 누수 조건과의 격차 | 69.0%p | 48.3%p |

회수율이 66.7% → 58.3%로 떨어진 것은 후보가 7배로 늘어 경쟁이 심해진 결과다.
절대 회수 건수는 8 → 14건으로 늘었다. **병목이 커버리지에서 검색 정밀도로 이동했다.**

> **정정 (2026-08-19)** — "경쟁이 심해진 결과"라는 이 해석은 틀렸다. 코퍼스를
> 5,921건으로 더 키우자 회수율이 68.0%로 **올랐다**. 진짜 원인은 3,247건 코퍼스가
> 2025~2026년에 편중돼 있었던 것이다. 아래 8/19 항목 참조.

상세는 `results/검색_기준선_452건.md`.

**미완** — 품목분류사례 2,800/5,507건에서 중단. CLIP이 `pageUnit` 20 이상 요청을
거부하기 시작했다(per=10만 응답). 하루 요청량 누적에 따른 제한으로 보이며, 다음 세션에 이어받는다.

## 2026-08-19 — 수집 완료 + 코퍼스 5,921건

전날 중단된 품목분류사례 2,707건을 이어받아 **5,507건 전량 수집 완료**(실패 0, 중복 0).
`pageUnit` 거부는 서버 측 일시 장애였고 다음 날 `per_page=100`이 정상 응답했다.
재발에 대비해 연도별로 쪼개 요청하는 `collect_years()`를 쓴다 — 깊은 페이지를
요구하지 않는 것이 근본 대책이다.

| 계열 | 건수 |
|---|---|
| 협의회 | 133 |
| 위원회 | 386 |
| 품목분류사례 | 5,507 |
| 수집 원본 | **6,026** |
| 세번 추출 실패·물품설명 없음 제외 | -75 |
| 테스트셋 (기존 30건 그대로 재사용) | -30 |
| **검색 코퍼스** | **5,921** (기본 검색 대상 5,892) |

**어제 받은 2,800건은 최근 2년에 편중돼 있었다.** 이어받기에서 2024년 657건,
2025년 1,386건, 2026년 757건이 `건너뜀`으로 처리됐고 합이 정확히 2,800이다.
전 기간을 최신순으로 훑다 끊긴 결과였다.

### 검색 커버리지 (측정 코드: `search.py`의 `measure_coverage()`)

| 지표 | 3,247건 | 5,921건 | 변화 |
|---|---|---|---|
| 정답 6자리가 코퍼스에 존재 | 82.8% | **86.2% (25/29)** | +3.4%p |
| 검색 top-5 회수 | 48.3% | **58.6% (17/29)** | **+10.3%p** |
| 존재하는 것 중 회수율 | 58.3% | **68.0% (17/25)** | **+9.7%p** |
| 재정렬 사정권(12건) 교집합 | 5건 | **6건** | +1건 |
| 누수 격차 (top-5) | 48.3%p | **37.9%p** | -10.4%p |

**코퍼스를 1.8배로 키웠는데 정밀도까지 올랐다.** 3,247건 때 회수율이 66.7% → 58.3%로
떨어졌던 건 후보 경쟁 때문이 아니라 연도 편중 때문이었다. 과거 3년이 채워지면서
회수율이 68.0%로 되돌아왔다. 선례는 오래된 쪽에 더 쌓여 있다.

병목은 이제 검색 정밀도다 — k=20이면 22/29(75.9%)까지 오르지만 top-5에서는 17건이다.
커버리지는 거의 채웠고 남은 손실은 순위에서 난다. 상세는 `results/검색_기준선_452건.md`.

## 2026-08-19 — 4주차: [3] 재정렬 최초 측정

`results/20260819_112047/` · `gemini-3.1-pro-preview` · temperature 1 · 조건 A 29건
코퍼스 5,921건(평가셋 제외 기본 동작) · top_k=5 · 위반 시 채점은 **안 A**(모델 응답 그대로)

### 숫자

| 지표 | 값 |
|---|---|
| before Top1 6자리 (같은 실행, [1]단계만) | 55.2% (16/29) |
| **after Top1 6자리 ([3] 재정렬 후)** | **72.4% (21/29)** |
| after Top3 6자리 | 86.2% (25/29) |
| 안 B로 집계했을 때 | 72.4% (21/29) — 위반 0이라 안 A와 동일 |
| 규칙 위반 / 파싱 실패 | **0건 / 0건** |
| 실행 시간 | 1,916초 (66.1초/건, 건당 2콜) |
| 토큰 | in 113,901 / billed_out 130,416 |

**목표 65%(19건)를 넘었다.** CLAUDE.md 규칙에 따라 여기서 [3] 튜닝을 멈추고
다음 단계로 간다.

### 개선분을 정직하게 나누면

44.8% → 72.4% 를 통째로 재정렬 효과라고 말할 수 없다. 세 겹이다.

| 단계 | Top1 6자리 | 무엇이 바뀌었나 |
|---|---|---|
| baseline (Playground 수기) | 44.8% (13건) | — |
| 평가 자동화 + JSON 구조화 출력 | 51.7%(8/18) / **55.2%(8/19)** | 프롬프트·출력형식. 실행 간 ±1~2건 변동 |
| **+ [3] 재정렬** | **72.4% (21건)** | **+5건** |

**재정렬의 순수 기여는 +5건(+17.2%p)이다.** before를 같은 실행에서 함께 잰 것이
이 분해를 가능하게 했다. 다른 날 숫자와 비교했다면 temperature 1의 실행 간 변동이
개선분에 섞여 구분되지 않았을 것이다.

### 재정렬이 실제로 한 일

| 항목 | 값 |
|---|---|
| 순서를 바꾼 건 | 7건 (5, 11, 13, 14, 15, 17, 23) |
| 그중 Top1 승격 | **5건** (5, 13, 14, 15, 17) |
| Top1 강등 | **0건** |
| 검색이 정답을 물어온 건(17건) 중 Top1 정답 | **16건** |
| 검색적중인데 못 뒤집은 건 | 1건 (no=16) |

**강등이 0건이다.** 재정렬은 근거가 있을 때만 움직였고, 근거가 없을 때는
1차 순서를 그대로 뒀다. 프롬프트 규칙 4번("바꿀 이유가 없으면 그대로")이
의도대로 작동했다는 뜻이고, 일관성 지표에도 유리한 성질이다.

11번과 23번은 2·3위만 맞바꿔 Top1에 영향이 없었다.

### 확신도가 정답률과 상관이 있다

| 확신도 | 건수 | Top1 6자리 정답 |
|---|---|---|
| high | 21 | **19/21 (90.5%)** |
| medium | 6 | 2/6 (33.3%) |
| low | 2 | **0/2 (0%)** |

모델이 스스로 매긴 확신도가 실제 정답률을 가른다. 7주차 UI에서
**"확신도가 high가 아니면 사람이 반드시 확인"** 이라는 규칙의 근거로 쓸 수 있다.
다만 29건 1회 측정이므로 low가 2건뿐이다 — 단정하기엔 표본이 작다.

### baseline 대조 (Top1 6자리)

| 구분 | 건수 | 번호 |
|---|---|---|
| 유지_O | 12 | 3, 6, 8, 9, 10, 11, 13, 20, 22, 23, 28, 29 |
| **회수** | **9** | 5, 7, 12, 14, 15, 17, 26, 27, 30 |
| 손실 | 1 | 19 |
| 유지_X | 7 | 2, 4, 16, 18, 21, 24, 25 |

순증감 +8건. 손실 1건(no=19)은 재정렬 탓이 아니다 — [1]후보 생성에서 정답이
2위로 밀렸고, 검색이 근거를 못 물어와(검색적중 N) 재정렬이 손 쓸 수단이 없었다.

### 한계 — 이 숫자를 인용할 때 반드시 함께 적을 것

- **1회 실행이다.** temperature 1이므로 재현 시 ±1~2건 흔들린다. 일관성은 별도 측정한다
- **개발셋(평가셋 29건) 숫자다.** 5주차 목표 판정은 테스트셋 30건으로 하며,
  홀드아웃 규칙상 아직 열지 않았다
- 10자리는 측정하지 않았다. [4]세번 확정이 없어 6자리까지만 다룬다

## 2026-08-19 — [1] 후보 캐시 (비용 절감) + 위반 판정 버그 수정

성능 수치는 바뀌지 않는다. 앞으로의 실험 비용과 측정 해석에 영향을 준다.

**후보 캐시** (`pipeline.generate_candidates`) — 캐시 키는
`모델 + CANDIDATE_PROMPT 전문 + 물품설명`의 sha256이다. 프롬프트를 고치면
키가 달라져 캐시가 저절로 무효가 된다. 옛 프롬프트의 후보를 새 프롬프트
결과인 양 쓰는 것이 이 기능의 유일한 위험이고, 키 설계로 막았다.

flash 2건 검증: 2회차에서 `후보캐시_적중 2/2`, `billed_out 3,504 → 1,741`
(**-50.3%**), `before_top1_6`은 50.0%로 동일. 입력 토큰은 6,511 → 6,112로
거의 안 줄었다 — 입력의 대부분이 재정렬 프롬프트(결정례 5건)이기 때문이다.
**절감분은 전부 [1]단계의 thinking 토큰이다.**

부작용: 캐시를 켜면 `before` 숫자가 첫 실행 값에 고정된다. 의도한 것이며
([3]의 효과만 남기기 위해), [1]의 실행 간 변동을 재려면 `use_cache=False`.

**위반 판정 버그** — `rerank()`가 후보 코드를 정규화 없이 비교하고 있었다.
[1]이 `3210.00`을 주고 [3]이 `321000`으로 답하면 같은 코드인데
"후보 밖 코드 생성"으로 오탐한다. `normalize_code()`를 통과시키도록 고쳤다.
**8/19 재정렬 측정(위반 0건)은 영향을 받지 않았다** — pro가 점 없는 표기로만
답했음을 응답 원문에서 확인했다. 점을 섞는 것은 flash 쪽이었다.

## 2026-08-19 — 6자리별 10자리 목록 확보 (`data/hsk_codes.csv`)

[4] 세번 확정의 **선택지 목록**이다. API를 부르지 않으므로 성능 수치는 바뀌지 않는다.

**출처**: 공공데이터포털 · 관세청 「HS부호 단위별 품목명」 기준일자 2026-01-01.
5개 시트(2/4/6/8/10단위)를 `src/hsk.py`가 합쳐 10자리 11,327행으로 정제한다.
같이 받은 「HS부호」 파일(12,469행)은 쓰지 않는다 — 11,327 최종 10자리에
중간 마디 1,142개가 섞여 있고, 부가 열(적용일자·수량단위)은 [4]에 필요 없다.

**코퍼스 역산을 버린 이유**: 결정례 5,921건에서 사전을 만들면 6자리당 10자리가
평균 1.32종(1,170종 중 940종이 단 1개), 평가셋 정답 포함률 79.3%였다.
선택지가 빈약하면 **모델이 고를 게 없어서 맞히므로** [4] 성능이 부풀려진다.

**검증 결과** (`python -m src.hsk`)

| 항목 | 값 |
|---|---|
| 10자리 총 건수 | 11,327 (6자리 5,613종) |
| **평가셋 29건 정답이 목록에 존재** | **29/29** → [4] 이론 상한 100% |
| 6자리당 10자리 | 평균 **2.02종** (코퍼스 역산 1.32종) |
| 평가셋 정답 6자리의 선택지 | 평균 3.24개 · 최대 7개 · **1개뿐 8건** |
| 평가셋 정답 품목명이 3자 이하("기타" 등) | **17/29 (58.6%)** |
| 그중 상위설명이 비어 있는 건 | 0건 |

> **정정** — 계획 단계에서 적은 "평균 2.22종 / 선택지 평균 3.62개 / 최대 9개"는
> 틀렸다. 중간 마디 1,142개를 선택지에 섞어 센 값이다(12,469÷5,613=2.22).
> 실제 신고 가능한 10자리만 세면 2.02 / 3.24 / 7이다. 위 표가 옳다.

**설계에 미치는 영향 두 가지**

1. **선택지가 1개뿐인 8건은 6자리만 맞히면 10자리가 자동 확정된다.** [4]가 실제로
   판단하는 건 21건이다. 10자리 목표 40%(12건)는 이 21건에서 나와야 한다.
2. **품목명만 주면 [4]는 판단할 수 없다** — 정답의 58.6%가 "기타"다. 그래서
   `상위설명` 열에 4·5·6·7·8·9단위 이름을 ` > `로 이어 붙였다. 상위 자신이
   "기타"인 경우도 있으므로(`3917399000` → `391739 기타`) [4]에는 형제 항목까지
   보이도록 **6자리 아래 목록 전체**를 준다.

`data/hsk_codes.csv`는 3.7MB다(긴 4단위 품목명이 11,327번 반복된다). 커밋한다.
[4] 프롬프트에서는 상위설명을 그룹당 한 번만 출력해 토큰 반복을 피할 것.

## 2026-08-19 — [4] 세번 확정 추가 (flash 예비 측정, 10자리 13.8% → 65.5%)

`results/20260819_143550/` · `gemini-3.7-flash` · temperature 1 · 조건 A 29건 · top_k=5

**pro가 아니라 flash 숫자다.** [4] 프롬프트에 문제가 없는지 싸게 확인하려고 먼저 돌렸다.
baseline은 pro로 측정했으므로 **아래 숫자를 baseline과 직접 나란히 놓으면 안 된다.**

| 지표 | baseline | 이번 (flash) | 목표 |
|---|---|---|---|
| Top1 6자리 | 44.8% | 72.4% (21/29) | 65% |
| **10자리** | **13.8% (4건)** | **65.5% (19/29)** | 40% |
| **6→10 전환율** | **31% (4/13)** | **90.5% (19/21)** | 63% |
| 확정 위반(목록 밖 코드) | — | **0건** | — |

**세 가지로 쪼개 본 10자리**

| 구분 | 값 | 뜻 |
|---|---|---|
| 전체 | 19/29 (65.5%) | 목표를 재는 숫자 |
| 6자리 맞힌 건 중 | 19/21 (90.5%) | [4] 자체의 실력 |
| **그중 선택지 2개 이상** | **13/15 (86.7%)** | **모델이 실제로 고른 건만** |
| 자동확정(선택지 1개) | 9/29 (6자리도 맞은 건 6) | API 없이 결정 — [4]의 공이 아니다 |

틀린 2건도 뒷자리만 갈렸다 — no=28 `8544422090` → `8544422010`,
no=29 `1602329000` → `1602321090`.

**이 숫자를 자랑하기 전에 반드시 붙일 말**

1. **[4]는 생각보다 쉬운 과제였다.** 평가셋 정답의 58.6%가 품목명 "기타"이고,
   "특정 항목에 해당하지 않으면 기타"는 목록만 주면 거의 자동으로 따라온다.
   baseline 13.8%가 낮았던 진짜 이유는 모델이 **목록 없이 10자리를 지어냈기** 때문이다.
   개선의 큰 몫은 판단력이 아니라 **선택지를 준 것** 자체다.
2. **10자리의 병목은 이제 [4]가 아니라 6자리다.** 전환율이 90.5%이므로
   10자리를 더 올리려면 [3]·[1]에서 6자리를 더 맞혀야 한다.
3. 자동확정 9건을 성적에서 분리해 두었다. 빼고 봐도 86.7%다.
4. 1회 실행이고 개발셋 숫자다. 목표 판정은 테스트셋으로 한다(홀드아웃 미열람).

**비용**: 총 40.2분 · in 130,119 · billed_out 57,440 (후보 캐시 3/29 적중).
[4]의 입력은 건당 500~900토큰으로 재정렬(6,000+)보다 훨씬 싸다 — 선택지 목록에서
공통 상위설명을 헤더로 한 번만 출력하기 때문이다(`pipeline._format_options`).

## 2026-08-19 — [4] 세번 확정, pro 공식 측정 (10자리 13.8% → 69.0%)

`results/20260819_153150/` · `gemini-3.1-pro-preview` · temperature 1 · 조건 A 29건 · top_k=5

**목표 대비**

| 지표 | baseline | 이번 | 목표 | 달성 |
|---|---|---|---|---|
| Top1 6자리 | 44.8% (13) | **75.9% (22/29)** | 65% | O |
| **10자리** | **13.8% (4)** | **69.0% (20/29)** | 40% | O |
| **6→10 전환율** | 31% (4/13) | **90.9% (20/22)** | 63% | O |

**건별 diff (Top1 6자리, baseline 대조)**

| 구분 | 건수 | 번호 |
|---|---|---|
| 유지_O | 13 | 3, 6, 8, 9, 10, 11, 13, 19, 20, 22, 23, 28, 29 |
| 회수 | 9 | 4, 5, 7, 12, 14, 15, 17, 26, 30 |
| **손실** | **0** | — |
| 유지_X | 7 | 2, 16, 18, 21, 24, 25, 27 |

baseline이 맞힌 13건을 하나도 잃지 않았다. 순증감 +9건.

**10자리를 쪼개 보면**

| 구분 | 값 |
|---|---|
| 전체 | 20/29 (69.0%) |
| 6자리 맞힌 건 중 | 20/22 (90.9%) |
| **선택지 2개 이상, 모델이 실제로 고른 건** | **14/16 (87.5%)** |
| 자동확정(선택지 1개) | 9/29 |
| 확정 위반(목록 밖 코드) · 파싱 실패 | 0건 · 0건 |

**pro vs flash — 거의 같다**

| | flash | pro |
|---|---|---|
| Top1 6자리 | 72.4% (21) | 75.9% (22) |
| 10자리 | 65.5% (19) | 69.0% (20) |
| 전환율 | 90.5% | 90.9% |
| billed_out | 57,440 | **150,202 (2.6배)** |
| 소요 | 40.2분 | 42.5분 |

pro가 6자리에서 1건 더 맞히고 값은 2.6배다. **[4] 단계만 보면 차이가 없다**(전환율
90.5 vs 90.9). 7주차 배포 모델을 정할 때 근거로 쓴다.

**캐시**: `후보캐시_적중 0/29`는 정상이다. 캐시는 8/19에 도입했고 검증을 flash로만
했으므로 pro 후보는 이번에 처음 저장됐다(현재 캐시 58건 = flash 29 + pro 29).
다음 pro 실행부터 [1]은 공짜다.

**여기서 멈춘다 — 남은 병목은 [4]가 아니다**

전환율이 90.9%이므로 10자리를 더 올리는 길은 [4] 튜닝이 아니라 6자리를 더 맞히는
것뿐이다. 못 맞힌 7건(2, 16, 18, 21, 24, 25, 27)이 다음 과녁이고, 그중 4건은
Top3에도 정답이 없어 [3]으로는 손댈 수 없다. CLAUDE.md 스코프 통제에 따라
**목표를 넘겼으므로 추가 튜닝 없이 다음 단계로 간다.**

남은 검증: 개발셋 숫자다. 목표 판정은 테스트셋으로 하며 아직 열지 않았다.

## 2026-08-19 — 홀드아웃 테스트셋 1회차 열람 (총 2회 중 1회 소비)

`results/20260819_162513/` · `gemini-3.1-pro-preview` · temperature 1 · 30건 · top_k=5
[3]과 [4]가 모두 끝난 시점이라 **한 번의 열람으로 5·6주차를 동시 판정**했다. 1회 남았다.

### 판정

| 기준 | 목표 | 테스트셋 | 판정 |
|---|---|---|---|
| 5주차 Top1 6자리 | 60% | **56.7% (17/30)** | **미달** (1건 차이 — 18건이면 60%) |
| 6주차 10자리 | 30% | **50.0% (15/30)** | 달성 |

### 개발셋과의 격차가 크다 — 이게 이번 열람의 진짜 산출물이다

| 지표 | 개발셋(29건) | 테스트셋(30건) | 격차 |
|---|---|---|---|
| before Top1 6자리 ([1]단계만) | 58.6% | 53.3% | -5.3%p |
| **after Top1 6자리** | **75.9%** | **56.7%** | **-19.2%p** |
| Top3 포함 | 86.2% | 80.0% | -6.2%p |
| 10자리 | 69.0% | 50.0% | -19.0%p |
| 전환율 6→10 | 90.9% | 88.2% | -2.7%p |
| 검색 top-5 적중률 | 58.6% | 56.7% | -1.9%p |

**격차의 대부분은 [3] 재정렬에서 났다.**

| | 개발셋 | 테스트셋 |
|---|---|---|
| [3]의 기여 (before→after) | **+17.3%p (+5건)** | **+3.4%p (+1건)** |
| 순서를 바꾼 건 | 7 | 11 |
| Top1 승격 | 5 | 3 |
| **Top1 강등** | **0** | **2** |
| 검색이 정답을 물어온 건 중 Top1 정답 | 16/17 | **12/17** |

**개발셋의 "강등 0건"은 재현되지 않았다.** 검색적중률·전환율·Top3 포함률은 거의
같은데 [3]의 회수력만 무너졌다. 즉 병목은 검색도 [4]도 아니고 **[3]의 판단**이다.
검색이 근거를 물어다 줘도 5건에서 순위를 못 고쳤다(T10, T18, T19, T28, T29).

**과적합인가?** [3] 프롬프트를 개발셋으로 반복 튜닝한 적은 없다(한 번 써서 나온
숫자다). before부터 이미 5.3%p 낮은 것을 보면 테스트셋이 조금 더 어렵고,
개발셋 29건 1회 측정의 "강등 0"에 운이 섞여 있었다고 보는 편이 사실에 가깝다.
어느 쪽이든 **인용해야 할 숫자는 테스트셋 쪽이다.**

### 확신도는 테스트셋에서도 살아남았다

| 확신도 | 건수 | Top1 6자리 정답 |
|---|---|---|
| high | 24 | 17/24 (70.8%) |
| medium | 5 | **0/5** |
| low | 1 | **0/1** |

**medium·low 6건은 전부 오답이다.** 개발셋에서 본 패턴(high 90.5% / medium 33.3%
/ low 0%)이 다른 데이터에서도 재현됐다. 7주차 UI의
**"확신도가 high가 아니면 사람이 반드시 확인"** 규칙은 이제 두 데이터셋의 근거를 갖는다.
이게 정확도보다 실무에 가까운 산출물일 수 있다.

### 다음

CLAUDE.md 규칙대로 **일정을 연장하지 않는다.** 목표를 60% → 56.7%로 낮춰 기록하고
다음 단계로 간다. 남은 사정권은 7건(T5, T12, T21, T22, T23, T28, T29)이고
그중 검색이 근거를 물어온 건 2건뿐이다.

**비용**: in 134,046 / billed_out 157,717 / 31.5분.

## 2026-08-19 — 일관성 측정 (10건 × 3회, flash)

`results/20260819_171305` · `_174329` · `_194917` · `gemini-3.7-flash` · temperature 1
baseline과 같은 10건(6, 8, 9, 28, 29, 2, 12, 15, 17, 23) · **후보 캐시 OFF**

**일관성 = 3회가 서로 같은가**이지 정답인가가 아니다. 틀린 답을 3번 똑같이 해도 1이다.

| 지표 | baseline (pro) | 이번 (flash) | 목표 |
|---|---|---|---|
| 일관성 6자리 | 90% | **80% (8/10)** | — |
| **일관성 10자리** | **60%** | **80% (8/10)** | 90% → **미달** |
| 정답재현 6자리 (3회 중) | 1.9 | **2.6** | — |
| 정답재현 10자리 (3회 중) | 1.3 | **2.0** | — |

**모델이 다르다**(baseline pro / 이번 flash). 직접 비교로 단정하지 말 것.

### 가장 중요한 발견 — 6자리와 10자리 일관성이 정확히 같아졌다

baseline은 6자리 90%인데 10자리가 60%였다. **6자리가 같아도 10자리가 흔들렸다는 뜻**이다.
이번엔 둘 다 80%이고, 흔들린 건도 같은 2건(no=2, no=9)이다. 즉

> **6자리만 정해지면 10자리는 더 이상 흔들리지 않는다.**

목록을 주기 전에는 모델이 매번 10자리를 새로 지어냈고, 목록을 주자 그 변동이 사라졌다.
[4]의 기여는 정확도(13.8%→69.0%)만이 아니라 **재현성**이다. 실무에서는 이쪽이 더 중요하다 —
같은 물품을 두 번 물었을 때 다른 코드가 나오면 쓸 수 없기 때문이다.

### 흔들린 2건

| no | 3회 답 | 성격 |
|---|---|---|
| 2 | `3208909011` / `3210001099` / `3208909011` | 6자리부터 갈림 (320890 ↔ 321000) |
| 9 | `8424899000` / `8424899000` / `8424100000` | 6자리부터 갈림 (842489 ↔ 842410) |

**둘 다 [3] 재정렬 단계에서 6자리가 흔들린 것**이고, [4]가 흔든 게 아니다.

no=28, 29는 3회 모두 같은 답을 냈지만 **오답**이다(`8544422010` vs 정답 `8544422090`).
일관성 지표는 1이지만 정답률에는 기여하지 않는다 — 지표를 나눠 보는 이유다.

### 목표 미달 — 남은 수단은 temperature 하향

10자리 일관성 목표 90%에 80%로 미달했다. CLAUDE.md가 예고한 순서대로
**temperature 하향은 [3]·[4] 효과 측정이 끝난 지금 적용할 수 있다.**
먼저 온도를 낮췄다면 개선이 재정렬 덕인지 온도 덕인지 구분할 수 없었을 것이다.
지금은 그 구분이 끝났으므로 온도만 따로 움직일 수 있다.

### 부수 확인

- 후보 캐시 적중 0/10 × 3회 — 캐시 OFF가 의도대로 동작했다
- 비용: 3회 합계 billed_out 약 61,000 / 소요 약 95분

## 2026-08-20 — baseline 정정: 6자리 41.4% → 44.8% (내 숫자를 스스로 낮춤)

외부 리뷰에서 "엑셀로 세어 보면 13/29인데 기록은 12건"이라는 지적을 받고 원본을 뒤졌다.
**지적이 맞았다.** 원인은 측정이 아니라 엑셀 셀 하나다.

### 원인

`baseline_측정표.xlsx` `측정기록` 시트 **29행의 '정답 HSK' 셀(D열)에 값이 아니라 수식
문자열이 남아 있다** — `=IF(케이스목록!$D33="","",케이스목록!$D33)`. 그 셀을 참조해 계산되는
O/X 플래그 5개(P1 4·6·10자리, P2 Top1·Top3)가 **전부 0으로 굳었다.**

실제 값은 이렇다.

| 항목 | 값 |
|---|---|
| 정답 (케이스목록 D33) | `1602329000` |
| P1 코드 · P2 후보1 | `1602.32-1000` → 4자리·6자리 일치 |
| P2 후보2 | `1602.32-9000` → **10자리까지 정답** |
| 기록된 플래그 | 5개 전부 `0` |

**같은 행의 오답유형 태그가 `한국 고유 세번(10자리) 오류`다.** 태그 스스로 "6자리는 맞았고
10자리에서 틀렸다"고 말하고 있는데 플래그는 6자리도 오답이라고 하고 있었다. 이 모순이 증거다.

다른 28행과 `조건B_품명only`·`일관성` 시트에는 수식 문자열이 없다(전수 확인). **29행 단독 문제다.**

### 이미 한 번 이 숫자를 마주쳤었다

착수 전 압박 검증(`_grill/2026-08-18-hs코드-분류-검증.md`)에 이렇게 적혀 있다.

> 초안 CLAUDE.md와 달랐던 3개: 조건A 6자리 44.8 → **41.4**, P2 Top1 44.8 → **41.4**,
> Top3 86.2 → **82.8**. 각각 정확히 1건씩 부풀려져 있었다.
> 41.4·82.8을 내는 파일만 존재하고 44.8·86.2를 내는 파일은 없다.

**초안에 적었던 44.8·86.2가 맞는 값이었다.** 그때 "파일이 내는 값"을 신뢰해 낮춰 잡았는데,
그 파일이 내던 값이 깨진 플래그였다. "각각 정확히 1건씩"이라는 관찰까지 해놓고도 원인을
셀 단위로 파고들지 않은 것이 놓친 지점이다. 파일을 믿은 판단 자체는 옳았고, **파일의
어느 부분을 믿을지**(원자료 vs 파생 계산)를 구분하지 않은 것이 문제였다.

### 영향

| 지표 | 기록값 | 정정값 |
|---|---|---|
| 조건 A 4자리 | 51.7% (15/29) | **55.2% (16/29)** |
| **조건 A 6자리 / P2 Top1** | **41.4% (12/29)** | **44.8% (13/29)** |
| 조건 A 10자리 | 13.8% (4/29) | 13.8% (4/29) — 불변 |
| **P2 Top3 포함 = [3] 이론 상한** | **82.8% (24/29)** | **86.2% (25/29)** |
| 6→10 전환율 | 33% (4/12) | **31% (4/13)** |
| 재정렬 사정권 | 12건 | 12건 — 불변 |
| Top3에도 정답 없음 | 5건 | **4건** |
| 6자리는 맞고 10자리만 오답 | 8건 | **9건** |
| 한국 고유 세번 오류 6건 중 6자리를 이미 맞힌 건 | 4건 | **5건** |

**개선 폭이 줄어든다.** 기존 기록의 건별 diff 세 곳에서 no=29가 `회수` → `유지_O`로 옮겨간다.

| 실행 | 회수 | 손실 | 순증감 |
|---|---|---|---|
| 8/18 평가 자동화 | 3 → **2** | 0 | — |
| 8/19 [3] 재정렬 | 10 → **9** | 1 | +9 → **+8** |
| 8/19 [4] pro 공식 | 10 → **9** | 0 | +10 → **+9** |

**손실 0건은 세 실행 모두 그대로다.** 정정 전이든 후든 baseline이 맞힌 건을 잃지 않았다는
사실은 바뀌지 않는다 — 오히려 지켜낸 건수가 12 → 13으로 늘었다.

### 조치

`evaluate.load_baseline()`이 엑셀 플래그를 읽지 않고 **후보 코드에서 O/X를 다시 계산**한다.
정답도 계산식이 있는 `측정기록` D열이 아니라 `케이스목록` D열에서 가져온다. 재계산 결과가
기록된 플래그와 다른 행이 있으면 실행할 때마다 경고를 찍는다(지금은 29번 1건).

**원본 xlsx는 고치지 않았다.** CLAUDE.md의 "baseline 절대 수정 금지"를 지키는 것이기도 하고,
원본을 고쳐 버리면 "무엇이 어떻게 틀려 있었는지"를 나중에 확인할 수 없기 때문이다.
틀린 기록은 지우는 것보다 남겨두고 옆에 정정을 붙이는 편이 낫다.

### 남는 교훈

**스프레드시트의 파생 계산은 원자료가 아니다.** 이번 채점 자동화가 처음부터 후보 코드에서
직접 채점했다면 애초에 생기지 않았을 문제다. 사람 손을 거친 중간 집계값을 기준선으로 삼을 때는
원자료에서 한 번 다시 계산해 대조해야 한다.

## 2026-08-20 — 회귀 세트 20건 신설 (강등 1건 / 승격 1건, 통과)

`results/20260820_111529/` · `gemini-3.1-pro-preview` · temperature 1 · 20건 · top_k=5

### 왜 만들었나

**홀드아웃 30건이 전부 협의회 계열이다.** 협의회는 분류가 갈려 논의에 올라간 건이므로,
지금까지 **어려운 건만으로 측정해 왔고 쉬운 건이 깨지는지는 아무도 재지 않았다.**

[3]재정렬은 결정례를 근거로 순위를 뒤집는 구조다. 쉬운 건은 [1]후보가 이미 맞으므로,
관련 없는 결정례가 검색되면 **맞던 걸 틀리게 만들 수 있다.** 실제로 홀드아웃에서
강등 2건이 나왔다. 지인에게 배포했을 때 "쉬운 것도 틀리는데요"가 나오면 끝이다.

### 구성

품목분류사례(일반 사전심사) 5,440건 중 평가셋 아님 · 물품설명 100자 이상인 4,813건에서
**빈도 가중 10건 + 류 층화 10건**, 시드 고정(20260820).

처음에는 "류별 층화 20건"으로 뽑았는데 **크기와 무관하게 모든 류가 같은 확률**이 되어
코퍼스의 54.4%를 차지하는 상위 8개 류(84·85·39·90·23·38·73·21)가 **하나도 안 뽑혔다.**
평가셋이 39·84류에 몰려 있는데 정작 그 계열이 감시망 밖에 있는 셈이라 방식을 바꿨다.
하이브리드로 다시 뽑아 39·73·85류가 들어왔고, 18개 류 · 2022~2026년에 분포한다.

**코퍼스에서 제외** — 행을 지우지 않고 `결정례.csv`·`embeddings_meta.csv`에 `회귀셋` 열을
추가했다(평가셋 누수 통제와 같은 방식). 행 순서·건수가 그대로라 **임베딩 재생성이 없었다**(비용 0).
`search.search()`는 `include_eval` 과 무관하게 회귀셋을 항상 뺀다 — 그 스위치의 용도는
평가셋 누수 격차 측정 하나뿐이기 때문이다.

기본 검색 대상 5,892건 → **5,872건**. 개발셋 커버리지는 그대로였다
(top-5 회수 58.6%, 존재 중 회수율 68.0%, 누수 격차 37.9%p — 20건을 빼도 소수점까지 같다).

### 결과

| 지표 | 회귀셋 20건 | 개발셋 29건 | 홀드아웃 30건 |
|---|---|---|---|
| **before Top1 6자리 ([1]단계만)** | **85.0% (17/20)** | 58.6% | 53.3% |
| after Top1 6자리 | 85.0% (17/20) | 75.9% | 56.7% |
| **승격 / 강등** | **1 / 1** | 5 / 0 | 3 / 2 |
| Top3 포함 | 100% (20/20) | 86.2% | 80.0% |
| 10자리 | 80.0% (16/20) | 69.0% | 50.0% |
| 전환율 6→10 | 94.1% (16/17) | 90.9% | 88.2% |
| 검색적중률 | 70.0% | 58.6% | 56.7% |
| 규칙 위반 · 파싱 실패 | 0 · 0 | 0 · 0 | 0 · 0 |

**판정: 통과** (기준: 강등 0~1건). 순증감 0건이다.

**before 85.0%가 전제를 확인해 준다.** "품목분류사례는 선례가 명확해 상대적으로 쉽다"는
가정이 데이터로 뒷받침됐다(개발셋 58.6% / 홀드아웃 53.3% 대비 +26~32%p).
회귀 세트로 성립한다는 뜻이다.

비용: in 87,376 / billed_out 99,282 / 12.5분. 후보 캐시 적중 0/20(신규 20건이므로 정상).

### 강등 1건(R7)이 유사도 임계값 가설을 깬다

```
R7  정답 200989
    [1] 후보:  200989 | 210390 | 130219   ← 1순위가 이미 정답
    [3] 결과:  130219 | 200989 | 210390   ← 3순위를 1순위로 끌어올렸다
    검색 1위 유사도 0.9146 (정답이 아닌 세번)  /  정답 결정례 0.8639
```

재정렬은 **점수가 더 높은 결정례를 근거로** 맞던 답을 뒤집었다.
0.9146은 높은 점수이므로 **임계값을 0.7로 걸든 0.8로 걸든 이 건은 통과한다.**
"점수 낮은 결정례를 빼면 강등이 줄어든다"는 가설은 최소한 이 사례에 해당하지 않는다.

이번에 추가한 점수 로깅으로 20건 전체를 보면 더 분명하다.

| 구분 | 건수 | 검색 1위 유사도 범위 |
|---|---|---|
| 검색이 정답을 물어온 건 | 14 | 0.769 ~ 0.964 |
| 못 물어온 건 | 6 | 0.748 ~ **0.944** |

**두 분포가 거의 완전히 겹친다.** 못 물어온 건 중 최고점(R17, 0.9442)이 물어온 건 중
최저점(R11, 0.7688)보다 훨씬 높다. **유사도 점수는 그 결정례가 쓸모 있는지를 알려주지 않는다.**

개발셋 29건에도 같은 로깅을 돌려 확인해야 확정이지만, 지금 데이터로는 5단계 임계값이
**"갈리지 않으므로 접는다"** 로 끝날 가능성이 높다. 접더라도 이 표가 산출물이다 —
"유사도를 필터로 쓰려 했으나 데이터가 지지하지 않았다"는 것도 결과다.

### 한계

- **20건 1회 측정이다.** temperature 1이므로 강등 1건은 재현되지 않을 수 있다.
  판정 기준을 "강등 0~1건 통과"로 미리 정해 둔 것은 이 흔들림을 감안한 것이다
- 홀드아웃 1회차(56.7%)는 코퍼스 5,892건 기준이고 앞으로의 측정은 5,872건 기준이다.
  **조건이 다르므로 나란히 인용할 때 이 문장을 함께 적는다**
- 회귀 세트에는 열람 횟수 제한을 두지 않았다. 목표 판정에 쓰지 않기 때문이다

## 2026-08-20 — 유사도 임계값: 채택하지 않음 (API 비용 0으로 내린 결론)

리뷰에서 "강등을 막는 가장 싼 방법"으로 제안받은 것이 유사도 임계값이었다.
0.7 미만이면 결정례를 아예 넣지 않는 식이다. **측정해 보니 지지되지 않았다.**

유사도 점수는 LLM이 아니라 검색만으로 나오므로 **LLM 호출 없이** 검증했다
(임베딩 질의 29건 1회). 개발셋 점수는 오늘 다시 계산하고, 승격/강등 판정은
8/19 pro 실행(`results/20260819_153150/`) 결과와 결합했다. 그 사이 코퍼스가
5,892 → 5,872건으로 바뀌었지만 회수 건수는 17건으로 동일해 결합이 성립한다.

### 근거 1 — 점수가 "쓸모 있는 결정례"를 가려내지 못한다

| 데이터셋 | 검색 적중 건 점수 범위 | 비적중 건 점수 범위 |
|---|---|---|
| 개발셋 29건 | 0.761 ~ 0.896 (중앙 **0.832**) | 0.670 ~ 0.860 (중앙 **0.762**) |
| 회귀셋 20건 | 0.769 ~ 0.964 | 0.748 ~ **0.944** |

개발셋은 중앙값이 갈리지만 **범위가 겹친다.** 회귀셋은 거의 완전히 겹친다 —
못 물어온 건 중 최고점(0.944)이 물어온 건 중 최저점(0.769)보다 훨씬 높다.
**점수가 높다는 것과 그 결정례가 이 물품에 맞다는 것은 별개다.**

### 근거 2 — 실제 강등이 고득점에서 났다

회귀셋의 유일한 강등 R7은 **검색 1위 유사도 0.9146**이었다(정답 결정례는 0.8639).
임계값을 0.7로 걸든 0.8로 걸든 **이 건은 통과한다.** 막으려던 바로 그 사고를 못 막는다.

### 근거 3 — 막을 것은 없는데 잃을 것은 있다

개발셋에서 임계값별로 결정례가 통째로 잘리는 건수:

| 임계값 | 잘리는 건 | 그중 검색이 정답을 물어왔던 건 |
|---|---|---|
| 0.70 | 2 | 0 |
| 0.75 | 3 | 0 |
| 0.80 | 10 | **2** |
| 0.85 | 24 | **13** |

승격 5건(no=5, 12, 14, 15, 17)의 점수는 0.828 ~ 0.868로 **전부 고득점**이다.
0.75 이하 임계값은 무해하지만 **개발셋 강등이 0건이라 막을 대상 자체가 없다.**
0.80 이상은 적중 건을 버리기 시작한다.

덧붙여 두 데이터셋의 점수 분포 자체가 다르다(회귀셋이 전반적으로 높다 —
품목분류사례는 평범한 물품이라 가까운 이웃을 잘 찾는다). **하나의 고정 임계값이
데이터셋마다 다르게 작동한다**는 뜻이라 더욱 쓰기 어렵다.

### 결론

**임계값을 도입하지 않는다.** [3]에 `min_score` 인자를 넣지 않았고, 계획에 잡아 뒀던
5-2단계(pro 98콜 / 50분 + flash 196콜)를 **집행하지 않는다.**

**아이디어를 접은 것도 결과다.** "유사도로 근거의 품질을 거를 수 있다"는 그럴듯한 가설이
세 가지 근거로 기각됐고, 그 검증에 든 LLM 비용은 0이다. 강등을 줄이려면 임계값이 아니라
**[3] 프롬프트가 1차 순위를 뒤집을 조건 자체를 좁혀야 한다** — 다음 과녁은 그쪽이다.

홀드아웃 30건의 강등 2건 점수는 계산하지 않았다. 계산해서 임계값을 정하면 그게 곧
홀드아웃 튜닝이다. 마지막 열람 때 결과와 함께 보고만 한다.

## 2026-08-20 — [0] 되묻기 게이트 신설 (품번only 14.3% → 57.1%, 목표 70% 미달)

`results/20260820_114041` · `_114304` · `_114533` · `gemini-3.1-pro-preview` · temperature 1

### 왜 지금인가

baseline에서 가장 뼈아픈 발견이 **정보부족 인지율 3.6%**(28건 중 1건)였는데 시스템에
반영되지 않은 채였다. 그리고 파이프라인이 조건 A(상세 물품설명)만 받는다 —
**실무에서 관세사에게 도착하는 것은 `D-100 Wash Solution` 같은 품명이다.**
이 상태로 지인에게 배포하면 쓸 수가 없다. 그래서 UI보다 앞에 두었다.

CLAUDE.md대로 이 단계의 목적은 정확도가 아니라 **안전성**이다. 조건 A와 조건 B 사이에
유의미한 정확도 차이가 없었으므로(A만 맞은 3건 / B만 맞은 4건), 되묻기로 정확도가
오를 것이라 기대하지 않는다. 확신에 차서 틀린 답을 주지 않게 하는 것이 전부다.

### 구현

`pipeline.gate()` — 짧은 프롬프트 LLM 1콜, JSON `{충분, 부족항목, 질문}`.
**파싱 실패 시 충분=True(fail-open).** 게이트가 깨졌다고 분류까지 막으면 도구가 아무 답도
못 하는 상태가 된다. 게이트는 안전장치이지 관문이 아니다.

`run_rerank_batch()`에는 넣지 않았다. 6자리 정확도에 게이트가 섞이면 지금까지의
before/after 분해가 무너진다. 게이트는 `app.py`에서만 앞단에 붙이고 측정은 따로 한다.

### 프롬프트를 한 번 갈아엎었다

**1차 — "분류를 가르는 항목(재질·용도·형태·구성)이 비어 있으면 불충분"**

| | 1차 | 목표 |
|---|---|---|
| 품번only 되묻기 | 100% (7/7) | 70% |
| **품명명확 오작동** | **90.5% (19/21)** | 30% 이하 |

**사실상 전부 되묻는다.** 한쪽 지표만 보면 만점인데 도구로는 쓸모가 없다.
원인은 기준 자체였다 — HS 분류에서는 **거의 모든 물품이** 재질·용도를 더 알면 정확해지므로
"항목이 비었나"로 물으면 항상 불충분이 된다. baseline에서 품명명확 21건의 조건 B 6자리가
52.4%였다는 것은 **절반은 품명만으로도 맞혔다**는 뜻이고, 그 건들은 통과시켜야 한다.

**2차 — "입력만 읽고 이 물품이 무엇인지 특정할 수 있는가" 하나만 본다.**
세부 정보 부족은 분류 뒤 확인 사항으로 넘기라고 명시했고, 예시 5개를 넣었다.
**예시는 평가셋에서 뽑지 않고 지어냈다** — 조건 B 28건이 곧 채점 대상이므로 거기서
예시를 가져오면 그 순간 측정이 오염된다.

시행착오는 전부 flash로 했다. pro로 했으면 같은 결론에 비용만 2.6배였다.

### 결과 (pro)

| 데이터셋 | 입력 성격 | 되묻기율 | 목표 | 판정 |
|---|---|---|---|---|
| **조건 B 품번only 7건** | 품명 원문 | **57.1% (4/7)** | 70% | **미달** |
| **조건 B 품명명확 21건** | 품명 원문 | **4.8% (1/21)** | 30% 이하 | **달성** |
| 조건 A 29건 | 상세 물품설명 | **3.4% (1/29)** | 낮을수록 좋음 | 달성 |
| 회귀셋 20건 | 상세 물품설명 | **0.0% (0/20)** | 낮을수록 좋음 | 달성 |

baseline은 품번only 14.3%(1/7) · 품명명확 0% · 질문 평균 0.07개였다.
→ **품번only +42.8%p**, 질문 평균 0.21개. 파싱 실패 0건.

**상세 설명 49건(조건 A 29 + 회귀셋 20)에서 되묻기가 1건뿐이다.** 배포했을 때 게이트가
정상 입력을 막아 세우지 않는다는 뜻이고, 이게 오작동 쪽 안전 근거다.

### flash와 pro가 28건 전건 같은 판정을 냈다

조건 B 28건에서 불일치 0건, 되묻기율도 소수점까지 동일하다. [4]세번 확정에서 본 것과
같은 패턴이다 — **선택지가 좁은 판단은 모델 급이 결과를 가르지 않는다.**
**배포 시 게이트는 flash로 돌려도 된다.** 공개 URL의 호출당 비용을 줄이는 근거다.

### 목표 미달 — 낮춰 기록한다

70%(5/7)에 1건 모자란 57.1%(4/7)다. 통과된 3건은 이렇다.

| 통과된 품번only | 판단 |
|---|---|
| `Silica Coating Liquid; NS1033011 Grey Primer` | 실리카 코팅액·회색 프라이머라는 **실제 물품 서술이 있다.** 레이블 쪽이 의심스럽다 |
| `D-100 Wash Solution` | "세척액"까지만 특정된다. 되물었어야 한다 |
| `중심봉147N(분체); C1-G5047-60; CN` | "중심봉"까지만 특정된다. 되물었어야 한다 |

그리고 유일한 품명명확 오작동 `MEGAD PC-WR`는 **사실상 품번**이다. 게이트가 되물은 쪽이 맞다.

**레이블 오류 2건을 감안하면 실질 성적은 더 높지만, 인용하는 숫자는 57.1%다.**
채점 기준을 사후에 유리하게 바꾸면 baseline 정정으로 얻은 신뢰를 스스로 깎는다.
프롬프트를 더 비틀지 않는다 — 1차 시도에서 품명명확 오작동이 90.5%까지 치솟는 것을 이미 봤다.
CLAUDE.md 규칙대로 목표를 70% → 57.1%로 낮춰 적고 넘어간다.

### 덤 — 게이트가 평가셋의 결함을 잡아냈다

조건 A에서 되물은 1건은 오작동이 아니었다. 물품설명이
`"상기 성분으로 혼합된 황색∼갈색계 점조액상 / 용도: 식용"`인데 **그 "상기 성분"이 입력에 없다.**
원본 결정례에서 발췌할 때 위쪽 성분표가 잘려 나간 건이고, 게이트는
"'상기 성분'이 구체적으로 무엇인지 알려 달라"고 정확히 되물었다.
**정보 부족을 재려고 만든 것이 실제로 정보 부족을 찾아냈다.**

### 한계

- 조건 B 28건은 게이트 프롬프트를 고칠 때 본 데이터다. **개발셋 숫자**로 다뤄야 한다.
  다만 프롬프트 수정은 1회였고, 상세 설명 49건의 오작동율은 그 튜닝과 무관한 확인이다
- 품번only가 7건뿐이다. **1건이 14.3%p를 움직인다**
- 레이블(품명명확/품번only)에 최소 2건의 경계 사례가 있다

---

## 2026-08-20 — 재정렬 입력을 얼마나 줄일 수 있나

pro 일일 쿼터가 소진돼 그날 남은 시간을 **입력 비용을 줄일 여지가 있는지** 확인하는 데 썼다.
두 가지를 잰다 — 결정례에서 **결정사유를 빼면** 어떻게 되나, 결정례를 **더 많이 넣으면**
어떻게 되나. 둘 다 flash로 했고, 판정 기준은 측정 전에 적어 두었다.

### 실측 — pro 일일 쿼터는 250 requests/day 다

429 응답 본문에 `GenerateRequestsPerDayPerProjectPerModel`, `quota_value: 250`,
`retry_delay: 74568초`(약 20.7시간)가 찍혀 있었다. **크레딧을 충전해도 풀리지 않는다.**
CLAUDE.md 1주차의 "무료 티어 한도 실측" 항목이 여기서 답을 얻었다.

중요한 건 이 쿼터가 **토큰이 아니라 요청 수 기준**이라는 점이다. 아래 두 실험은
입력 토큰을 줄이지만 **하루에 돌릴 수 있는 건수는 1건도 늘리지 못한다.**

### 실험 1 — 결정사유 ablation (`results/20260820_141725`, `20260820_142302`)

`REASON_CUT`을 상수에서 `_format_cases(reason_cut=)` → `rerank()` → `run_rerank_batch()`
인자로 내렸다. 상수를 고쳐 두 번 돌리지 않은 이유는, 그러면 조건이 코드 히스토리에만 남고
**결과 파일에 안 남기 때문**이다. 지금은 `summary.json`의 `결정사유컷`만 봐도 조건을 안다.

| 지표 | 결정사유 600자 (현행) | 결정사유 제거 |
|---|---|---|
| before_top1_6 | 58.6% (17/29) | 58.6% (17/29) |
| **after_A_top1_6** | **72.4% (21/29)** | **69.0% (20/29)** |
| 승격 | 4건 | 3건 |
| 강등 | 0건 | 0건 |
| after_A_top1_10 | 65.5% (19/29) | 62.1% (18/29) |
| 총 in_tokens | 120,565 | **69,536 (-42.3%)** |

`before`가 두 조건에서 완전히 같다 — [1]이 캐시에서 나왔다는 뜻이고,
**차이가 오직 [3]에서만 왔다**는 확인이다.

**차이는 1건, no=12 하나다.**

```
정답 200899 | 검색 top-5 안에 정답 결정례 있음 (유사도 0.8318)
  1차후보  : 200600 | 200899 | 210690
  사유 600 : 200899 | 200600 | 210690   -> 정답
  사유 제거: 200600 | 200899 | 210690   -> 1차 순서 그대로
```

**이 1건이 이 실험에서 제일 값진 부분이다.** 두 조건 모두 검색은 성공했고,
결정례의 **결정세번 `200899`는 프롬프트에 똑같이 들어 있었다.** 코드만 봤을 때는 순위를
바꾸지 않았고, **왜 그렇게 분류했는지를 읽고 나서야** 바꿨다. 확신도는 양쪽 다 `high`였다 —
모델은 틀렸을 때도 확신했다.

즉 [3]이 결정례를 쓰는 방식은 "비슷한 사례의 코드를 베끼는 것"이 아니라 **판단 근거를 읽는 것**이다.

**판정: 유지.** 미리 정한 기준은 "1건 하락 = 애매 → 회귀셋으로 재확인"이었으나,
그 기준은 절감분이 의미 있다는 전제에서 나온 것이었고 전제가 깨졌다 — 쿼터가 요청 수 기준이라
입력을 42% 줄여도 병목이 안 풀린다. **요청 수는 그대로, 정확도만 3.4%p 손해.**
회귀셋을 더 돌려도 이 계산이 바뀌지 않으므로 재확인 없이 확정했다(flash 80콜 절약).

### 실험 2 — top_k 5 → 20 (LLM 호출 0건)

실험 1에서 "모델이 결정례 본문을 실제로 읽는다"가 확인됐으므로, 다음 질문은
**더 많이 넣으면 회수율 58.6%를 넘길 수 있나**였다. LLM을 부르기 전에 임베딩만으로 먼저 쟀다.

| k | 회수 | **사정권 교집합** | 프롬프트 평균 |
|---|---|---|---|
| 5 (현행) | 17/29 | **6/12** | 5,697자 |
| 8 | 17/29 | 6/12 | 8,503자 |
| 10 | 18/29 | 6/12 | 10,340자 |
| 15 | 20/29 | 7/12 | 15,078자 |
| 20 | 22/29 | **8/12** | 19,809자 |

가운데 열이 판단 근거다. 전체 회수율이 아니라 **[3]이 실제로 뒤집을 수 있는 12건 중
몇 건을 물어왔나**다. k=10에서 회수가 17→18로 올라도 사정권은 6 그대로라 [3]에게는
아무 일도 일어나지 않는다.

```
k=5   회수된 사정권: 5, 12, 14, 15, 16, 26
k=20  회수된 사정권: 5, 12, 14, 15, 16, 21, 26, 27      <- +2건
```

**k를 4배로 올려서 버는 건 2건이다.** 그리고 실험 1의 승격 4건(12,14,15,26)이 전부 이
회수된 6건 안에 있었다 — **[3]의 사정권 전환율은 4/6 = 67%**. 그대로 적용하면
k=20의 기대 이득은 **+1.3건(+4.5%p)** 이다.

여기에 개발셋→홀드아웃 **전이율 실측 20%** ([3]: dev +17.3%p → test +3.4%p)를 곱하면
홀드아웃 기대 이득은 **+0.9%p ≈ 0.27건**이다. **홀드아웃 1건이 3.3%p이므로 기대 이득이
1건에 못 미친다.** 재도 "올랐다"고 말할 수 없는 크기이고, 이건 홀드아웃 최종 열람 **전에만**
넣을 수 있는 변경이라 되돌릴 기회가 없는 자리에 강등 리스크를 얹는 셈이다.

비용은 오히려 문제가 아니었다 — 남은 pro 작업 전부(온도 13 + 일관성 30 + 홀드아웃 30)에
적용해도 추가 입력은 +468k 토큰이고 요청 수는 불변이다.

**판정: 기각.** flash 58콜을 아꼈다.

**이 표는 ⑤ 검색 개선(결정사유 인덱스)의 기대 상한도 함께 보여준다.**
검색을 아무리 고쳐도 이 코퍼스에서 회수 천장이 22/29이고, 거기서 [3]이 실제로 쓰는 건 8/12다.

### 부수 — 배치 내구성 (성능 무관)

이날 배치가 세 번 죽었고 그중 한 번은 pro 16건치 응답(in 72k / out 85k)이
`summary.json` 없이 통째로 날아갔다. `run_rerank_batch()`에 건별 중간 저장과
`resume=` 인자를 넣었다.

- 결과 폴더를 **시작할 때** 만든다. 끝에서 만들면 도중에 죽었을 때 산 응답이 사라진다
- CSV는 모든 값을 문자열로 돌려주는데 파이썬에서 `"False"`는 **참**이다. 그대로 집계하면
  오답을 정답으로 세어 **정확도가 100%로 나온다.** `_restore_row()`가 형을 되돌린다
- 검증은 완료된 회귀셋 실행을 통째로 이어받아 원본 `summary.json`과 **전 지표 일치**로 했다(API 0콜)

실제로 이 실험 1이 도중에 끊겼을 때 13건을 재구매 없이 이어받았다.

---

## 2026-08-20 — 실무자 확인: 실제 입력은 카탈로그 PDF·사진이다 (미구현, 기록만)

지인 실무자에게 들은 것 — **품목분류를 할 때 물품을 확인하는 경로가 대개
카탈로그 PDF 나 제품 사진**이다. 텍스트로 물품설명을 타이핑하는 상황이 아니다.

이 지적은 baseline 의 "정보를 더 줘도 정확도가 안 오른다"로 반박되지 않는다.
그 비교는 **둘 다 물품을 서술한 두 텍스트**(조건 A 상세설명 vs 조건 B 품명원문)
사이의 것이었다. 카탈로그가 필요한 상황은 **서술이 아예 없는 경우**다.

숫자로도 겨냥점이 분명하다.

| | 건수 |
|---|---|
| 조건 B 28건 중 품번only | 7건 (25%) |
| 그중 게이트가 되물은 건 | 4건 (57.1%) |
| **되물은 뒤 사용자가 할 수 있는 것** | **없음** |

**게이트가 현재 막다른 길이다.** "정보를 더 달라"고 했는데 사용자에게 있는 건
카탈로그뿐이다. 이건 부가 기능이 아니라 지금 시스템이 못 푸는 25% 를 정면으로 겨냥한다.

### 그런데 지금 넣지 않는다

1. **홀드아웃 열람이 1회 남았다.** 인용할 숫자를 만든 시스템과 배포한 시스템이
   달라지면 그 숫자를 인용할 수 없다. 파이프라인 변경은 최종 열람 전에 끝나야 한다
2. **측정할 방법이 없다.** 평가셋 30건에 카탈로그 이미지가 없다. 지금 붙이면
   "넣었다"만 있고 "얼마나 나아졌다"가 없는 기능이 된다. 이 프로젝트가 내세우는
   것과 정확히 반대다
3. CLAUDE.md 스코프 통제에 `이미지 입력` 이 명시돼 있다

### 하기로 한 방식 (홀드아웃 확정 이후)

**[0-a] 카탈로그 → 물품설명 초안** 을 **앞단에 얹는다.** 이미지에서 텍스트를 뽑아
입력창을 채우고, 그 뒤는 기존 파이프라인이 그대로 받는다. 그러면 텍스트 경로의
홀드아웃 숫자가 그대로 유효하다.

자리는 **되묻기 화면**이다. 사용자가 막힌 바로 그 지점에 두는 것이 값이 가장 크다.

Gemini 가 이미지·PDF 를 그대로 받으므로 **새 라이브러리가 필요 없다.**
`st.file_uploader` → bytes → `llm.ask()` 에 미디어 인자 하나. 반나절 분량이다.

**업로드 파일은 저장하지 않는다.** 남의 카탈로그는 제3자 영업자료다.
뽑아낸 텍스트만 DB 에 남기고 원본은 버린다.

---

## 2026-08-20 — 온도 하향(1.0 → 0.2): 기각

`results/20260820_172739/` · `gemini-3.1-pro-preview` · 조건 A 29건 · top_k=5

CLAUDE.md 가 예고한 순서대로, [3]·[4] 효과 측정이 끝난 뒤에 따로 적용해 봤다.
판정 기준도 CLAUDE.md 에 미리 적혀 있었다 — **"6자리·10자리가 떨어지면 되돌린다."**

| 지표 | 온도 1.0 (8/19) | 온도 0.2 (8/20) |
|---|---|---|
| before_top1_6 ([1]만) | 58.6% (17/29) | 55.2% (16/29) |
| **after_A_top1_6** | **75.9% (22/29)** | **72.4% (21/29)** |
| after_A_top3_6 | 86.2% (25/29) | 89.7% (26/29) |
| **after_A_top1_10** | **69.0% (20/29)** | **62.1% (18/29)** |
| 전환율_6to10 | 90.9% (20/22) | 85.7% (18/21) |
| 총 billed_out | 150,202 | **95,579 (-36%)** |

**판정: 기각. 온도 1.0 을 유지한다.**

### 갈린 건은 3건뿐이다

| no | 온도 1.0 | 온도 0.2 |
|---|---|---|
| 4 | 6·10자리 정답 | 오답 |
| 30 | 6·10자리 정답 | 오답 |
| 16 | 오답 | 6자리 정답 (10자리는 양쪽 오답) |

6자리 순증 −1, 10자리 순증 −2 다.

### 나빠진 것은 [3]이 아니라 [1]이다

**재정렬의 순수 기여는 양쪽 모두 +5건으로 같다**(17→22, 16→21).
움직인 것은 `before`, 즉 [1] 후보 생성이다(17→16). 온도를 세 단계 모두에
같은 값으로 넘기므로 [1]도 함께 낮아졌고, 거기서 1건을 잃었다.

10자리 2건 손실도 [4] 탓이 아니다. 두 실행에서 **6자리가 같았던 건은 10자리도
전부 같았다.** 잃은 no=4·30 은 6자리부터 틀린 건이고, [4]는 6자리가 틀리면
무슨 짓을 해도 틀린다.

### 남는 한계 — 정직하게 적는다

온도 하향의 원래 목적은 정확도가 아니라 **일관성**이었다. 그런데 여기서는
**정확도 기준으로 기각했고, 온도 0.2 의 일관성은 재지 않았다.**

그 이유는 두 가지다.
1. 판정 기준을 측정 전에 CLAUDE.md 에 적어 두었고, 결과를 보고 기준을 바꾸지 않는다
2. 양쪽 온도의 일관성을 다 재려면 pro 180콜이 필요한데 일일 한도가 250 이다.
   그러면 홀드아웃 최종 열람(88콜)을 같은 날 못 돌린다

**따라서 "온도 0.2 가 일관성을 올렸을 수도 있다"는 가능성은 배제되지 않았다.**
인용할 일관성 숫자는 온도 1.0 으로 잰 것이다.

부수 관찰 — 온도 0.2 에서 출력 토큰이 36% 줄었다(thinking 감소). 병목은
요청 수(250/day)이지 토큰이 아니므로 처리량에는 도움이 되지 않는다.

## 2026-08-20 — 홀드아웃 테스트셋 2회차 열람 (최종. 남은 열람 0회)

`results/20260820_180456/` · `gemini-3.1-pro-preview` · temperature 1 · 30건 · top_k=5
· 결정사유 600자 · 파싱실패 0 · 위반 0 · 확정위반 0 · 11.7분

1회차(8/19)와 **모든 조건을 동일하게 고정**했다. 조건을 바꾸면 차이를 파이프라인
탓으로 읽을 수 없게 된다. `[1]` 후보는 1회차에 pro·온도 1.0 으로 산 것이
**30/30 캐시 적중**해 그대로 재사용됐다. 즉 이번에 새로 돈 것은 `[3]`과 `[4]` 뿐이다.

### 최종 판정 (CLAUDE.md 목표 대비)

| 기준 | 목표 | 결과 | 판정 |
|---|---|---|---|
| 5주차 Top1 6자리 | 60% | **56.7% (17/30)** | **미달** (1건 차이) |
| 6주차 10자리 | 30% | **50.0% (15/30)** | **달성** |

**목표를 낮춰 기록하고 다음 단계로 간다.** 일정을 연장하지 않는다(CLAUDE.md 5주차 규칙).

### 집계는 1회차와 완전히 같다

| 지표 | 1회차 (8/19) | 2회차 (8/20) |
|---|---|---|
| before Top1 6자리 (`[1]`만) | 53.3% (16/30) | 53.3% (16/30) |
| **after Top1 6자리** | **56.7% (17/30)** | **56.7% (17/30)** |
| Top3 포함 (6자리) | 80.0% (24/30) | 80.0% (24/30) |
| **10자리** | **50.0% (15/30)** | **50.0% (15/30)** |
| 전환율 6→10 | 88.2% (15/17) | 88.2% (15/17) |
| 검색 top-5 적중 | 56.7% (17/30) | 56.7% (17/30) |
| 승격 / 강등 | 3 / 2 | 3 / 2 |

### 비율이 아니라 건별로 대조했다 — 그래서 이게 재현이다

비율만 같고 맞힌 건이 다를 수 있으므로 두 `detail.csv` 를 건별로 비교했다.

| 대조 항목 | 다른 건수 |
|---|---|
| Top1 6자리 O/X | **0 / 30** |
| Top1 10자리 O/X | **0 / 30** |
| `[4]` 가 고른 10자리 코드 자체 | **0 / 30** |
| `[3]` 재정렬 **순서** | 3건 (T14, T21, T25) |

순서가 바뀐 3건도 **전부 2·3순위끼리의 교체**다. 1순위는 30건 전건 동일했다.
승격은 T4·T14·T20, 강등은 T5·T21 로 1회차와 같고, 1회차에 "검색이 근거를
물어다 줘도 순위를 못 고쳤다"고 적은 5건(T10, T18, T19, T28, T29)도 그대로다.
남은 사정권 7건(T5, T12, T21, T22, T23, T28, T29)도 집합이 같다.

**`[1]`이 고정된 상태에서 `[3]`·`[4]`가 온도 1.0 으로 30건을 다시 판단했는데
결론이 한 건도 안 바뀌었다.** 일관성을 pro 로 따로 재지 못한 상황에서, 이건
30건 규모의 재현 증거로 쓸 수 있다. 다만 아래 두 가지는 분명히 해 둔다.

- 이건 **`[3]`+`[4]`의 재현**이지 파이프라인 전체의 재현이 아니다. `[1]`은 캐시라
  다시 굴리지 않았다. 실제 앱은 `[1]`도 매번 새로 부른다(`use_cache=False`)
- 일관성 지표(같은 입력 3회)와는 측정 대상이 다르다. 인용할 일관성 숫자는
  여전히 flash 로 잰 6자리 80% / 10자리 80% 다

### 나빠진 것 — 확신도 라벨이 위험한 방향으로 흔들렸다

| 확신도 | 1회차 건수 → Top1 6자리 정답 | 2회차 건수 → 정답 |
|---|---|---|
| high | 24 → 17 (**70.8%**) | 26 → 17 (**65.4%**) |
| medium | 5 → **0** | 3 → **0** |
| low | 1 → **0** | 1 → **0** |

**"high 가 아니면 사람이 반드시 확인" 규칙은 살아 있다** — high 가 아닌 4건은
이번에도 전부 오답이다(1회차 6건도 전부 오답). 개발셋·홀드아웃 1·2회차,
세 번 모두 재현됐다.

문제는 반대쪽이다. 라벨이 바뀐 건 4건인데 방향이 나쁘다.

| 건 | 1회차 → 2회차 | 정답 여부 |
|---|---|---|
| T5 | medium → **high** | 오답 |
| T21 | medium → **high** | 오답 |
| T28 | medium → **high** | 오답 |
| T7 | high → medium | 오답 |

**오답 3건이 medium 에서 high 로 올라왔다.** 코드와 10자리는 한 건도 안 바뀌었는데
확신도만 흔들렸고, 그 결과 high 의 정밀도가 70.8% → 65.4% 로 떨어졌다.
안전망이 잡아 주던 3건을 놓친 셈이다.

→ **UI 문구에 이 숫자를 같이 적는다.** "high 면 안전하다"가 아니라
*"high 가 아니면 거의 확실히 오답이다(10건 전건). high 라도 3건 중 1건은 틀린다"*
가 두 번의 열람이 지지하는 문장이다. 확신도는 **거르는 장치이지 보증이 아니다.**

### 비용

in 124,159 / billed_out **78,262** / 11.7분.
1회차(in 134,046 / out 157,717 / 31.5분) 대비 출력 토큰이 **절반**이다.
`[1]` 30건이 전부 캐시 적중이라 pro 가 `[3]`+`[4]`만 돌았다.

### 이제 홀드아웃은 못 연다

CLAUDE.md 홀드아웃 규칙상 2회를 다 썼다. 앞으로 파이프라인을 고쳐도
**그 개선을 이 테스트셋으로 판정할 수 없다.** 인용할 최종 숫자는
**6자리 56.7% / 10자리 50.0%** 이고, 이 값을 만든 구성은 다음과 같이 확정한다.

```
모델   [3] gemini-3.1-pro-preview / [0][1][4] gemini-3.7-flash (배포 기준)
온도   1.0
검색   top_k 5 · 코퍼스 5,872건 (평가셋 29 + 회귀셋 20 제외)
입력   결정사유 600자 컷
```

배포한 앱과 인용한 숫자가 같은 시스템이어야 하므로, 이후 변경은 README 에
"홀드아웃 측정 이후의 변경"으로 따로 표시한다.

## 2026-08-21 — [0-a] 카탈로그 입력 신설 (봉인 이후의 변경 — 정확도 검증 불가)

`gemini-3.7-flash` · 새 의존성 0

실무자 확인에서 나온 "실제 입력은 카탈로그 PDF·제품 사진"을 구현했다. 봉인
2회를 다 쓴 뒤라 **이 기능의 정확도는 판정할 수 없다.** 그 사실을 먼저 적는다.

### 파이프라인 앞단에만 붙였다

```
카탈로그 → [0-a] 초안 → 사람이 확인·수정 → [0] 게이트 → [1]~[4]
                                          └ 여기부터 한 줄도 안 바꿨다
```

`pipeline.py` 변경은 **110줄 추가, 삭제 0줄**이다. 게이트·재정렬·세번확정
프롬프트 3개를 커밋본과 바이트 단위로 대조해 동일함을 확인했다. `[0]` 이후를
건드리면 6자리 56.7% / 10자리 50.0% 가 무효가 되는데 다시 잴 방법이 없다.

### 새 의존성이 0이다

`google-generativeai` 가 PDF·이미지를 그대로 받는다. pypdf 도 OCR 도 필요
없었다. `llm.ask(parts=...)` 에 `{"mime_type":..., "data": bytes}` 를 실어
보내는 게 전부다. `genai.upload_file()` 은 쓰지 않았다 — 구글 쪽에 파일
리소스를 만들어 두는 방식이라, 업로드한 카탈로그를 남기지 않겠다는 결정과
어긋난다.

### 호출 1회로 끝낸다

제품 목록과 각 제품의 초안을 **한 번에** 받는다. "목록 먼저 → 고른 뒤 → 초안"
으로 나누면 파일을 두 번 보내야 하는데, 이미지·PDF 는 입력 토큰을 많이 먹어서
그게 더 비싸다.

### 시험 파일 3개 결과

| 파일 | 시간 | in / out | 결과 |
|---|---|---|---|
| 사진 ① 로봇청소기 (홍보 문구만) | 13.7초 | 1,559 / 747 | **재질을 지어내지 않음.** 빠진정보 5개 보고 |
| 사진 ② 와인셀러 (사양표) | 5.4초 | 1,559 / 704 | 스테인리스·원목·700×1793×735·381L 정확 |
| PDF 전자도어락 (3쪽, 5.3MB) | 7.8초 | 2,063 / 974 | DC 6V·AA 4개·13.56MHz RFID 까지 |

**용량이 아니라 쪽수로 과금된다.** 5.3MB PDF 3쪽이 입력 2,063 토큰이다.

뽑은 초안 3개가 모두 `pipeline.gate()` 를 통과했다(충분=True). 앞단과 뒷단이
맞물린다는 뜻이다.

**사진 ①이 이 기능의 진짜 시험대였다.** 감성 카피뿐이고 사양이 없는 페이지인데,
모델이 재질을 그럴듯하게 지어냈다면 그 가짜 재질로 `[1]`~`[4]` 가 성실하게
분류하고 사용자는 어디서 틀어졌는지 모른다. 두 번 돌렸고 두 번 다 재질을
비워 두고 `빠진정보` 로 보고했다.

### 여러 제품 처리

와인셀러는 모델이 둘(W409NDR / W400ND)인데 **하나로 묶고 규격에 나열했다.**
프롬프트의 "색상·용량만 다른 모델 변형은 하나로 묶어라" 가 의도대로 작동했다.
문 열림 방향은 세번을 가르지 않으므로 이게 맞다.

파일 3개를 한꺼번에 올리면 제품 3개가 잡히고 `selectbox` 가 뜬다(브라우저에서
확인). 실제 다제품 종합 카탈로그는 아직 시험하지 못했다.

### 화면·안전장치

- 업로더는 **접힌 상자**로 입력창 위에 둔다. 측정된 경로는 여전히 글로 넣는 쪽이다
- 초안은 **편집 가능한 입력창**에 넣는다. 자동으로 분류로 넘기지 않는다
- 못 채운 항목을 **입력창 아래에 상시 표시**한다. 상자 안에도 있지만 그건
  접으면 사라진다. "재질은 내가 채워야 한다"가 안 보이면 빈 채로 분류가 돈다
- 추출 상한은 분류와 **별개**(세션 10회). 초안을 다시 뽑는 게 벌칙이 되면
  마음에 안 드는 초안을 그냥 쓴다. 게이트를 따로 세는 것과 같은 이유다
- **업로드 파일은 저장하지 않는다.** 남는 건 뽑아낸 텍스트뿐이다
- `.streamlit/config.toml` 신설 — `maxUploadSize = 10`. 기본값 200MB 라
  위젯이 "200MB per file" 이라 말해 우리 안내("합계 10MB")와 어긋나 보였다

### 남긴 기록

`runs` 표에 열 4개를 더했다. `입력출처` 를 **텍스트 / 카탈로그 / 카탈로그(수정)**
로 나눈 것이 핵심이다 — 사용자가 초안을 그대로 썼는지 손봐야 했는지가 남는다.
정확도를 잴 수 없는 이 경로에서 **품질을 짐작할 유일한 단서**다.

### 통합 확인

브라우저에서 초안 → 분류까지 돌려 `8301.30-0000`(가구용 자물쇠)을 받았고,
DB 에 `입력출처=카탈로그`, `빠진정보=["재질","외형 치수","중량"]`,
추출 토큰 4,247/2,365 가 기록됐다. 추출 상한 소진 시 버튼이 잠기는 것도 확인했다.

---

## 2026-08-21 — 실행 기록을 Supabase(Postgres)로 (정확도 무관)

날짜 / 변경 내용 / Top1 6자리 / 10자리 / 이전 대비
`2026-08-21 / 저장소 SQLite → Supabase / 변동 없음 / 변동 없음 / [0]~[4] 미변경`

**정확도와 무관한 변경이다.** `[0]`~`[4]` 를 한 줄도 건드리지 않았고,
`app.py` 에서 바뀐 것은 저장 호출을 감싸는 부분뿐이다.

### 왜 했나 — 편의 문제가 아니다

Streamlit Community Cloud 의 파일시스템은 휘발성이다. 카탈로그 기능을 배포하면서
**지인들에게 받은 첫 실사용 기록이 통째로 사라졌다.** 배포 환경에는 셸이 없어
사라지기 전에 꺼낼 방법도 없었다 — 조회 기능을 붙이려면 재배포해야 하고,
재배포하면 그 데이터가 지워진다.

문제는 **봉인 열람 2회를 다 썼다는 것**이다(2026-08-19, 08-20). 남은 측정 채널은
"실무자가 실제로 넣은 물품설명 + 👍/👎" 하나뿐인데, 그게 재배포 한 번에 지워지면
채널이 아니다. 기록이 남아야 이게 채널이 된다.

### CLAUDE.md 스코프 통제 위반 — 알고 넣었다

「사용자 인증, **DB**, 다국어, 이미지 입력」 항목에 정면으로 걸린다. 원래 규칙의
취지는 "지인 3명 규모에 인프라를 얹지 마라" 였고, 그 판단은 **측정 채널이 아직
남아 있을 때 옳았다.** 봉인을 다 쓴 지금은 전제가 바뀌었다.

같은 이유로, 이번에 함께 옮길 수 있었던 **일일 카운터는 옮기지 않았다.**
그건 편의(리셋 방지)일 뿐 측정 채널이 아니다. 스코프를 여는 근거는 하나뿐이고
그 하나에만 쓴다.

### 설계

`config.DATABASE_URL` 하나로 갈린다. 있으면 Postgres, 없으면 지금까지의
SQLite 파일. **로컬 `.env` 에는 넣지 않는다** — 내 시험 실행이 지인들의 실제
기록과 같은 표에 쌓이면 안 된다. 배포 기록은 Supabase 대시보드에서 본다.

- 드라이버는 `psycopg2-binary`. Supabase 파이썬 클라이언트(PostgREST)를 안 쓴 것은
  RLS 정책·REST 추상화가 얹혀 CLAUDE.md 의 "추상화 금지"에 어긋나기 때문이다
- 접속은 **Session pooler**(`*.pooler.supabase.com:5432`). 직접 접속
  (`db.*.supabase.co`)은 IPv6 전용이라 Streamlit Cloud 에서 닿지 않는다
- `storage.py` 에서 분기하는 방언은 셋뿐이다 — 자리표시자(`?` / `%s`),
  자동 증가 PK(`AUTOINCREMENT` / `SERIAL`), 열 목록(`PRAGMA` / `information_schema`).
  `COLUMNS` 의 타입과 한글 열 이름은 양쪽에서 그대로 통한다
- `conn.execute()` 를 전부 커서로 바꿨다. psycopg2 연결에는 `.execute()` 가 없다

### 원격 DB 라서 새로 생긴 문제 둘

1. **`DB_준비()` 가 화면을 다시 그릴 때마다 돌았다.** 로컬 파일이면 공짜지만
   원격이면 버튼 한 번에 TCP+TLS 접속과 `information_schema` 조회가 붙는다.
   세션 플래그로 한 번만 돌게 했다. `@st.cache_resource` 를 안 쓴 이유는
   그대로다(2026-08-20 기록) — 새로고침하면 풀려야 복구 경로가 산다.

2. **저장이 실패하면 화면 전체가 스택트레이스가 됐다.** pro 1콜을 쓴 분류
   결과까지 같이 사라진다. 로컬 파일에서는 사실상 안 일어나던 일이지만
   네트워크 DB 는 끊긴다. `try/except` 로 감싸 **fail-open** 으로 바꿨다 —
   게이트와 같은 원칙이다. 예외 메시지가 아니라 **종류 이름만** 노출한다.
   접속 실패 메시지에는 호스트·사용자명이 섞여 나온다.

### 남은 것

- 보관기간·삭제절차·접근통제가 여전히 없다. 이제 제3자(Supabase/AWS)에
  **영속 저장**되므로 미룬 값이 더 커졌다
- 지인 재시험 요청 시 "입력한 물품설명이 저장된다"를 알려야 한다
- Supabase 무료 프로젝트는 7일 무접속이면 일시정지된다. 10/13 전에 깨워 둘 것

### 실제로 걸린 함정 — 시크릿 이름

**연결에 두 번 실패했고, 두 번 다 코드 문제가 아니었다.**

Supabase 의 Connect 화면(ORM 탭)은 접속 문자열을 **`DIRECT_URL`** 이라는 이름으로
보여준다. 그건 Prisma 가 마이그레이션용으로 쓰는 변수명이다. 그대로 Streamlit
Secrets 에 넣었더니 우리 코드가 읽는 `DATABASE_URL` 이 비어 있었고, **조용히
SQLite 로 떨어졌다.** 화면은 완전히 정상으로 보였다 — 분류도 되고 👍 도 저장됐다.

여기서 판정을 한 번 틀렸다. 👍 위젯이 뜬 것을 보고 "Supabase 연결 성공"이라고
했는데, 그건 **저장이 됐다**는 증거일 뿐 **어디에 저장됐는지**는 말해 주지 않는다.
Supabase 대시보드에서 `relation "runs" does not exist` 가 나오고서야 드러났다.

그래서 두 가지를 고쳤다.

1. `DB_준비()` 가 붙은 저장소를 **로그에 한 줄 남긴다.** 조용한 fail-open 은
   앱을 살리지만 **실패를 안 보이게 만든다.** 되살릴 수 있게 하되 말은 해야 한다
2. `print(..., flush=True)` — 파이썬은 출력이 터미널이 아니면 버퍼에 모아 둔다.
   flush 없이는 배포 로그에 아무것도 안 찍혔다. 진단 수단이 진단이 안 됐다

교훈은 **"화면이 정상인 것"과 "의도한 경로로 도는 것"은 다른 명제**라는 것이다.
확인할 수단을 코드에 심어 두지 않으면 구분할 방법이 없다.

### RLS

`runs` 표에 `alter table runs enable row level security` 를 걸었다. 표를 만든
주인(`postgres`)으로 붙기 때문에 앱은 영향을 받지 않고, Supabase 의 공개 REST
경로(anon key)로 읽는 것만 막힌다. anon key 가 저장소에 있는 것은 아니지만,
**저장소가 공개라 프로젝트 주소는 이미 알려져 있고** 표에는 사용자가 넣은
물품설명이 들어간다. 한 줄로 막을 수 있는 것을 열어 둘 이유가 없다.

---

## 2026-08-21 — 카탈로그 경로의 첫 실사용 흔적 2건 (측정 아님)

배포 앱에 쌓인 초기 기록을 지우기 전에 옮겨 적는다. **정확도 측정이 아니다** —
봉인 2회를 다 써서 `[0-a]` 카탈로그 경로는 판정할 시험이 없다. 다만 이 두 건이
그 경로가 실제로 동작한다는 유일한 흔적이라 남긴다.

| | 입력 | 결과 | 평가 | 소요 |
|---|---|---|---|---|
| A | 자동차 사진 | `8703.23-1010` | 👍 "상세 설명이 없음에도 근접했음." | 98.3초 |
| B | 실버바 사진 | `7106.91-9000` | 👍 | 110.0초 |

**A 가 중요한 이유** — 뽑힌 초안은 이랬다.

> "전면 라디에이터 그릴에 현대 엠블럼이 부착된 흰색 승용 자동차로, 승객 수송 등의
> 용도로 사용되는 차량 형태를 띠고 있습니다."

그리고 `빠진정보` 에 다섯 항목을 올렸다 — 상세 모델명 / 재질 및 성분 /
**배기량 및 원동기 사양** / 차량 규격 및 치수 / 승차 정원.

**배기량은 8703호에서 세번을 가르는 항목이다.** 지어냈으면 그럴듯한 오답이
나왔을 것이고, 사용자는 어디서 틀어졌는지 알 수 없었을 것이다.
`CATALOG_PROMPT` 의 "카탈로그에 없는 정보를 지어내지 마세요 + 빠진정보 배열"이
실전에서 작동한 사례다. 추출 토큰은 1,531 in / 943 out.

### 두 가지 관찰 — 둘 다 n=2 다

**확신도가 봉인과 반대로 나왔다.** 두 건 다 `low` 인데 👍 를 받았다.
봉인 30건에서는 high 가 아닌 답 10건이 **전부 오답**이었고, 그래서 화면에
"이 답은 틀렸을 가능성이 높습니다"라고 적어 두었다. 표본 2건으로는 아무것도
뒤집히지 않지만, 계속 이 방향이면 그 문구가 맞는 답을 버리게 만드는 셈이다.
지인 재시험에서 이 열을 먼저 본다.

**카탈로그 입력이 두 배 넘게 느리다.** 98~110초 대 텍스트 26~41초.
`[0-a]` 호출이 하나 더 붙고 뽑힌 설명이 길어 뒤 단계도 같이 느려진다.
110초는 사용자가 창을 닫는 시간이다. 피드백에 "느리다"가 나오면 원인은 여기다.

---

## 2026-08-24 — 0단계: Render 무료 티어 실측 (정확도 측정 아님)

Streamlit → FastAPI + React 이전을 결정하고, **코드를 쓰기 전에 배포처부터 쟀다.**
1주차에 Gemini 무료 한도를 먼저 실측했던 것과 같은 순서다. 다 만들고 나서
"안 올라간다"를 알면 남은 기간이 통째로 날아간다.

`api/probe.py` (엔드포인트 4개, 나중에 지운다) · Render 무료 · Python 3.11.9 ·
`https://hskcodechecker.onrender.com`

### 잰 것 여섯

| # | 항목 | Render (리눅스) | 로컬 (Windows 참고) | 판정 |
|---|---|---|---|---|
| ① | 기동 직후 RSS | 59.0~59.2 MB | — | — |
| ② | 데이터 적재 후 RSS | **185.0 MB** / 512 MB | 183.2 MB | **통과** (여유 327MB) |
| ③ | 적재 소요 | **11.4초** | 1.7초 | 로컬의 7배 |
| ④ | 120초 침묵 요청 | HTTP 200 @ 120.4초 | — | **안 끊김** |
| ⑤ | 180초 침묵 요청 | HTTP 200 @ 180.3초 | — | **안 끊김** |
| ⑥ | 콜드스타트 | **22.9초** | — | 문서상 30~60초보다 빠름 |

SSE 도 함께 봤다 — `/sse?sec=120` 이 5초 간격 24틱을 **실제로 흘려보냈다.**
중간 프록시가 버퍼링하지 않는다는 뜻이다. 120초 내내 RSS 185.1MB 로 고정, 누수 없음.

데이터 무결성도 확인했다. 결정례 5,921건 / 벡터 `[5921, 768]` / HSK 11,327행 —
로컬과 전건 일치. 임베딩 18MB 와 메타 13MB 가 온전히 올라갔다.

### 배포 실패 2건 — 같은 실수를 반복하지 않으려고 남긴다

**(1) `ModuleNotFoundError: No module named 'fastapi'`**

Render 기본 빌드 명령은 루트 `requirements.txt` 를 쓴다. 그런데 fastapi 는
`api/requirements.txt` 에 있다 — streamlit 을 빼려고 일부러 나눈 파일이다.
Build Command 를 `pip install -r api/requirements.txt` 로 바꿔 해결.

파일을 나눈 판단 자체는 유지한다. API 서버에 streamlit 을 설치할 이유가 없다.

**(2) Python 3.14.3 이 잡혔다**

로그 경로가 `/opt/render/project/python/Python-3.14.3/` 이었다. 검증한 건 3.11.9 다.
`google-generativeai==0.8.6` 은 지원 종료 패키지라 3.14 동작을 아무도 보증하지 않는다.
환경변수 `PYTHON_VERSION=3.11.9` 로 고정.

**`.python-version` 파일을 쓰지 않았다.** 그 파일은 저장소 루트에 놓이는데
같은 저장소를 Streamlit Cloud 도 본다. Render 하나 고치려고 살아 있는 앱을
흔들 이유가 없다. 대시보드 환경변수는 Render 에만 적용된다.

### 설계에 반영할 것 셋

**최악의 경우를 계산해 보면 144초다.**

```
콜드스타트 22.9 + 데이터 재적재 11.4 + 분류 최대 110 = 약 144초
```

콜드스타트 응답의 `가동초=2.2`, `적재됨=[]` 가 이걸 증명한다 — 프로세스가 정말
죽었다 새로 떴고, **잠들기 전 올려둔 임베딩이 전부 날아갔다.** 깨어난 뒤 첫
분류는 적재 11.4초를 다시 문다.

그래서 처음엔 120초까지만 재고 "통과"로 적으려 했으나, 144초가 미측정 구간으로
남는다. 180초를 추가로 쟀고 그것도 통과했다. **미측정 구간을 남긴 채 통과라고
적으면 나중에 실사용자가 거기 빠졌을 때 이 기록이 거짓말이 된다.**

1. **적재를 `lifespan` 으로 옮긴다.** FastAPI 가 앱을 띄울 때 한 번 도는 자리다.
   11.4초가 Render 의 기동 대기 구간에 흡수되고 사용자 요청은 순수 분류 시간만 쓴다
2. **SSE 를 쓴다.** 판단이 두 번 뒤집혔다 — 120초 통과를 보고 "타임아웃 회피용으로는
   불필요"로 낮췄다가, 144초 계산이 나오면서 되돌렸다. 180초까지 버티므로 지금은
   *타임아웃* 이 아니라 *진행 표시* 가 주된 이유다. 26~110초의 편차를 화면이
   구분해 주지 못하면 사용자가 새로고침을 누르고, 그러면 250 req/day 를 두 번 문다
3. **주간 점검에 API 깨우기를 더한다.** `src/weekly.py` 가 Supabase 만 찌르고 있다

### 정확도

**변동 없음.** 이번 작업은 껍데기를 갈아치우는 것이고 `src/` 파이프라인은 손대지
않았다. 봉인 열람 2회를 다 써서 측정 수단도 없다 — 테스트셋 56.7% / 50.0% 이
확정값이다.

---

## 2026-08-24 — gemini-3.7-flash 가 심하게 느려졌다 (정확도 측정 아님)

1단계에서 API 로 분류를 돌려 보다 발견했다. **우리 코드 변경으로 생긴 게 아니다** —
`pipeline.py` 와 `llm.py` 는 8/20 이후 손대지 않았다.

### 단계별 소요 (SSE 이벤트에서)

| 단계 | 모델 | 오늘 | 8/20 기록 |
|---|---|---|---|
| ① 후보 생성 | **flash** | **165.2초** | — |
| ② 결정례 검색 | (임베딩) | 1.0초 | — |
| ③ 재정렬 | pro | **26.6초** | — |
| ④ 세번 확정 | **flash** | 300초+ (안 끝남) | — |
| **3단계 합계** | | 134 / 228 / 165초 | **18.2초** (id 2, 텍스트) |

### flash 단독 2회 — 후보 생성 1콜

| 회차 | 소요 | 입력 토큰 | 출력 토큰 | 처리율 |
|---|---|---|---|---|
| 1 | 231.6초 | 176 | 945 | 4.1 tok/s |
| 2 | **656.8초** | 176 | 810 | **1.2 tok/s** |

**출력 토큰은 정상 범위다(810~945).** 토큰이 안 늘었는데 시간만 늘었으므로
"모델이 더 생각한다"가 아니라 **뱉는 속도 자체가 느리다.** thinking 예산이
올라간 것이라는 가설은 이 숫자로 기각된다.

`llm.py` 의 재시도(503/500/429 → 20초, 40초 대기)가 돈 것도 아니다.
서버 로그에 `! ... 재시도` 줄이 하나도 없다. 한 번에 231초, 657초가 걸렸다.

### 지목이 정확한 이유

**pro 는 멀쩡하고 flash 두 단계만 이상하다.** ①과 ④ 사이에 낀 ③(pro)만
26.6초로 정상이다. 네트워크나 껍데기 문제라면 pro 도 같이 느려야 한다.

### 이걸 찾은 경위 — 합계만 보고 있었으면 못 찾았다

합계 `elapsed` 만 볼 때는 "제일 비싼 ③ 재정렬(pro)이 느리겠지"라고 세 번
추정했다. 실제로는 **가장 싼 단계**가 시간의 85% 를 먹고 있었다.
SSE 이벤트에 단계별 소요를 실은 것이 pro 호출을 한 번도 더 쓰지 않고
원인을 지목했다.

### 대응 — 일단 두고 본다 (2026-08-24 결정)

지금 손대지 않는다. 후보였던 것과 기각 이유:

- **MODEL_DEV 를 pro 로** — ①④ 도 pro 로 돌리면 26초대로 돌아오지만, pro
  250 req/day 를 분류 1건당 3콜씩 먹어 하루 83건으로 준다. CLAUDE.md 의
  "조건 간 비교는 flash, 인용할 절대 숫자만 pro" 원칙과도 어긋난다
- **max_output_tokens 상한** — 출력 토큰이 이미 정상 범위라 원인이 아니고,
  답이 잘릴 위험만 생긴다
- **thinking_config 명시** — baseline 측정 조건(Thinking High)을 바꾸는 일이다

2단계(React 화면)는 API 호출이 거의 필요 없어 지금 막히지 않는다.
화면을 만드는 동안 회복되는지 보고, 그때 다시 판단한다.

**정확도는 변동 없음.** 느려진 것이지 답이 바뀐 게 아니다.

## 2026-08-25 — Streamlit → FastAPI + React 이전 완료 (정확도 변동 없음)

`api/main.py` + `web/` · Render 서비스 2개 · 파이프라인 무수정

**정확도는 재지 않았고 잴 수도 없다.** 봉인 열람 2회를 다 썼다. 그리고 잴 이유도
없다 — `src/pipeline.py` / `src/search.py` / `src/llm.py` 를 한 줄도 고치지 않았다.
바뀐 것은 그 함수들을 **누가 부르는가** 뿐이다(Streamlit 위젯 → FastAPI 엔드포인트).

### 왜 옮겼는가

정확도 개선이 봉인 소진으로 끝나고 7주차 「URL 존재」도 충족된 뒤, 남은 기간에
할 일이 "피드백 기다리기" 뿐이었다. 결과물의 성격을 바꾸기로 한 것이지
Streamlit 이 부족해서가 아니다. **CLAUDE.md 의 금지 규칙을 함께 고쳤다** —
규칙을 남겨둔 채 어기면 문서가 거짓말이 된다.

### 배포 구성

| 서비스 | 종류 | 내용 | 잠드나 |
|---|---|---|---|
| `hskcodechecker` | Render Web Service | `api/main.py` | 15분 무활동 → 잠듦 |
| `hskcodechecker-web` | Render Static Site | `web/` 빌드 결과 | **안 잠듦** (0.09초) |
| (기존) Streamlit Cloud | — | `app.py` — **동결** | — |

세 앱이 **같은 Supabase 표**를 쓴다. `유입` 열이 채널을 갈라 준다.

**한 서비스로 합치지 않은 이유** — FastAPI 가 React 파일까지 뿌리면 배포도 하나고
CORS 도 필요 없다. 대신 화면이 API 와 같이 잠들어 **첫 방문자가 23초 흰 화면**을
본다. 그건 고장으로 읽힌다. 나눠 두면 화면은 즉시 뜨고 분류를 누를 때만 기다린다.

### 종단 시험 2회

| | 로컬 (8/25 05:25) | 배포 (8/25 10:27) |
|---|---|---|
| 입력 | 진공 단열 텀블러 | 가죽 방한 장갑 |
| ① 후보 생성 (flash) | 6.4초 | **61.4초** |
| ② 결정례 검색 | 0.5초 | 0.5초 |
| ③ 재정렬 (pro) | — | 33.1초 |
| ④ 세번 확정 (flash) | — | 23.8초 |
| **합계** | **25.6초** | **118.8초** |
| 결과 | `9617001000` | `4203292000` |

**재정렬이 일하는 장면이 하나 잡혔다.** 텀블러 건에서 정답 `9617.00` 은 검색
5건 중 **유사도 0.716 으로 꼴찌**였는데 [3] 이 이를 1위로 올렸다. 1위였던
`8509.40`(0.735, 이온수기)이 아니라. "모델은 답을 갖고 있고 순위만 틀린다"는
이 프로젝트의 전제가 눈앞에서 한 번 작동한 사례다.

### flash 지연은 절반만 가셨다

8/24 에 231~657초까지 갔던 `gemini-3.7-flash` 가 로컬 단문 호출에서 3.6초,
①에서 6.4초로 돌아왔다. 그러나 5시간 뒤 배포 환경 ①은 다시 61.4초였다.
**우리 코드가 아니라 구글 쪽 처리량 변동**이라는 8/24 진단은 유지된다
(같은 실행 안에서 pro 인 ③은 33.1초로 정상).

### 잡은 버그 — 오류를 처리하다 나는 오류

로컬 분류가 화면에서 영원히 "분류 중"으로 멈췄다. 연쇄는 이랬다.

```
④ finalize 가 DeadlineExceeded(504)
  → llm.py:94   재시도 안내 print 가 UnicodeEncodeError    ← '—' (em dash)
  → main.py:344 실패 로그 print 가 UnicodeEncodeError      ← except 블록 안
  → main.py:464 스트림 오류 print 가 UnicodeEncodeError    ← except 블록 안
```

윈도우 파이썬의 기본 출력 인코딩이 cp949 라 `—` 를 못 쓴다. **뒤의 둘이
`except` 안이었던 게 핵심이다** — 오류 처리기가 죽으면 그 아래가 통째로 안 돈다.
`save_run` 도 못 하고 `결과`/`오류` 이벤트도 못 내보내, 실패가 **정지처럼** 보였다.

`api/main.py` 진입점에서 `sys.stdout`/`sys.stderr` 를 UTF-8 로 재설정해 고쳤다.
`sys.stdout` 은 프로세스가 공유하므로 `src/` 의 print 도 같이 고쳐진다.

부수 발견 — **`llm.py` 의 재시도가 윈도우에서 한 번도 돈 적이 없다.** 504/500/429
에 20·40초 대기 후 재시도하게 돼 있었는데 그 안내 print 에서 죽어 시작조차 못 했다.
1주차부터 있던 코드인데 재시도가 발동한 적이 없어 드러나지 않았다.
Render(리눅스)는 기본이 UTF-8 이라 배포에서는 나지 않았을 문제다.

### 환경변수가 반영됐는지 대시보드 없이 확인하는 법

`ALLOWED_ORIGINS` 를 넣었는데 배포 화면이 계속 CORS 로 막혔다. 원인 갈라내기:

```
400  ← https://hskcodechecker-web.onrender.com   (새 화면)
200  ← http://localhost:5173                      (코드의 기본값)
```

**기본값이 아직 통과한다는 게 결정적이었다.** `os.getenv("ALLOWED_ORIGINS",
"http://localhost:5173")` 이므로, 변수가 들어왔다면 기본값은 목록에서 빠져
같이 400 이 나야 한다. 오타나 슬래시 문제였다면 두 줄 다 400 이었을 것이다.
즉 **변수가 프로세스에 도달하지 않았다**로 특정된다(다른 서비스에 넣었던 것).

넣고 나서는 정확히 뒤집혔다 — 배포화면 200 / localhost 400.

### 검증

| 항목 | 결과 |
|---|---|
| 게이트 되물음 → 「그래도 분류하기」 | ✓ |
| SSE ①②③④ 순차 갱신 + 단계별 초 | ✓ |
| 처리 중 오버레이 · 버튼 잠금 · `beforeunload` | ✓ |
| 결과 화면 + 근거 결정례 5건 | ✓ |
| 👍👎 즉시 저장 / 메모 저장 | ✓ |
| 세션 상한 5회 잠금 | ✓ (더미 행 주입으로 시험, API 소모 0) |
| 배포 DB 에 행 기록 (`유입` 포함) | ✓ |

**정확도 변동 없음.** 껍데기만 바뀌었다.

## 2026-08-25 — 지연이 pro 로도 번졌나 (8/24 관측의 후속, 정확도 측정 아님)

지인 몇 명이 시험해 보고 **"너무 오래 걸린다"** 고 알려 왔다. 8/24 에 flash
지연을 기록하며 "화면을 만드는 동안 회복되는지 보고 다시 판단한다"고
미뤄 둔 건이다. 회복되지 않았다.

### 실사용 6건 — 같은 입력 크기, 흩어진 시간

배포 DB `runs` 전건이다. 입력 토큰은 3,600~4,800 으로 고르다.

| id | 시각 | 물품 | 초 | out |
|---|---|---|---|---|
| 1 | 08-21 08:17 | 접이식 폴드 핸드폰 | 43.3 | 1954 |
| 2 | 08-25 10:27 | 가죽 장갑 | 118.8 | 2567 |
| 3 | 08-25 12:09 | 아이폰 16 | **16.7** | 1830 |
| 4 | 08-25 14:15 | P/N 4471-BK (예시 버튼) | 44.6 | 2113 |
| 5 | 08-25 14:48 | 이차전지용 구리 스크랩 | **261.2** | 2928 |
| 6 | 08-25 15:02 | 압전형 유리 파손 감지 센서 | **258.0** | 4441 |

### 단계별 (id 5 와 같은 입력을 배포에서 재현)

| 단계 | 모델 | 오늘 | 8/24 |
|---|---|---|---|
| ① 후보 생성 | flash | 72.8초 | 165.2초 |
| ② 결정례 검색 | (numpy) | **0.7초** | 1.0초 |
| ③ 재정렬 | pro | **131.5초** | 26.6초 |
| ④ 세번 확정 | flash | 0.0초 (선택지 1개, 자동확정) | 300초+ |
| 합계 | | 204.4초 | |

### 파이프라인 밖에서도 같다 — "1 더하기 1은?"

프롬프트 길이·복잡도를 배제하려고 최소 프롬프트로 쟀다. 출력은 전부
100 토큰 안팎이라 **뱉을 양이 같은데 시간만 튄다.**

| 모델 | 회수 | 최소 | 중앙 | 최대 |
|---|---|---|---|---|
| flash | 8 | 7.3초 | **41.3초** | **146.4초** |
| pro | 3 | 4.0초 | 13.6초 | 41.4초 |

flash 는 8회 중 7회가 10초를 넘었다.

### 무엇이 아닌지 (세 겹으로 배제)

- **우리 코드가 아니다.** 8/25 변경은 화면·기록·검색 계층뿐이고,
  그 ②단계는 0.7초로 멀쩡하다. `pipeline.py` 의 분류 경로는 손대지 않았다
- **Render 가 아니다.** 같은 ① 단계가 로컬에서 **108.8초** 로 배포(72.8초)
  보다 더 느렸다
- **재시도 대기가 아니다.** `llm.py` 의 20/40초 재시도가 돌면 로그에
  `! ... 재시도` 가 찍히는데 한 줄도 없다. 8/24 와 같은 확인이다

### 8/24 대비 — 결론은 유지, 단 pro 도 예전 같지 않다

**"flash 가 유독 느리다"는 지목은 오늘도 맞다** (중앙 41.3초 vs 13.6초).
다만 pro 도 한 번은 41.4초가 나왔고, ③ 재정렬은 131.5초였다. 재정렬은
결정례 5건을 함께 넣어 입력이 크므로 최소 프롬프트와 바로 비교할 수는 없다.
**pro 표본은 3회뿐이라 "번졌다"고 단정하지 않는다.**

### 대응 — 코드로 줄일 수 없다. 화면에서 기대치를 관리한다

8/24 에 기각한 선택지(MODEL_DEV → pro, max_output_tokens, thinking_config)는
그대로 기각이다. 대신 **기다리는 사람이 보는 것**을 고쳤다.

- 진행 표시에 **경과 초를 1초마다** 띄운다. 숫자가 움직이는 것 자체가
  "살아 있다"는 신호다
- 30초를 넘기면 "지금 모델 응답이 느립니다. 3분까지 걸릴 수 있습니다"
- 표시 조건을 `단계들` → `처리중` 으로. [0]게이트가 도는 동안에는 단계
  이벤트가 하나도 없어 **화면이 완전히 비어 있었다.** 가장 불안한 구간이
  거기였다

느려서 답답한 것과 **고장 난 줄 아는 것**은 다른 문제다. 뒤쪽만 고쳤다.

**정확도는 변동 없음.** 느린 것이지 답이 바뀐 게 아니다.

## 2026-08-27 — 지연이 저절로 가셨다 (8/24·8/25 관측의 결말, 정확도 측정 아님)

8/24 에 "일단 두고 본다", 8/25 에 "코드로 줄일 수 없다"로 두 번 미뤄 둔 건이다.
**우리가 아무것도 안 한 사이에 회복됐다.**

### 눈에 띈 것 — 실사용 2건이 거의 같은 입력인데 10배 차이

| id | 시각 | 설명길이 | in | out | 초 |
|---|---|---|---|---|---|
| 6 | 08-25 15:02 | 322 | 4805 | 4441 | 258.0 |
| **8** | **08-27 09:14** | **332** | **4894** | **3205** | **25.1** |

둘 다 카탈로그 경로, 같은 유리 파손 감지 센서, 같은 `gemini-3.1-pro-preview` 다.
**입력 토큰은 id 8 이 오히려 크다**(4894 > 4805). 출력이 28% 적지만 그걸로
10배는 설명되지 않는다.

### 초/출력토큰으로 정규화해도 차이가 남는다

배포 DB 8건을 생성량으로 나눠 보면 두 무리로 갈린다.

| 무리 | 초/출력토큰 | 해당 건 |
|---|---|---|
| 빠름 | 0.008 ~ 0.009 | id 8(08-27 09:14), id 3(08-25 12:09) |
| 중간 | 0.021 ~ 0.022 | id 1, id 4 |
| 느림 | 0.046 ~ 0.089 | id 2, id 5, id 6, id 7 |

**뱉은 양이 아니라 토큰당 속도가 달랐다.** 8/25 오후 세 건(14:48·15:02·15:29)이
전부 느린 무리지만, 오전 10:27(id 2)도 느려서 **시간대만으로는 설명되지 않는다.**

### 같은 잣대로 재측정 — 최소 프롬프트 "1 더하기 1은?"

8/25 와 같은 회수(flash 8회 / pro 3회), 같은 프롬프트다.

| 모델 | 8/25 중앙 → 오늘 | 8/25 최대 → 오늘 |
|---|---|---|
| flash | 41.3초 → **11.1초** | 146.4초 → **30.0초** |
| pro | 13.6초 → **2.8초** | 41.4초 → **3.9초** |

pro 3회가 2.8 / 2.8 / 3.9초로 **분산 자체가 사라졌다** (8/25 는 4.0 / 13.6 / 41.4).
실사용 경로가 pro 이므로 id 8 의 25.1초가 이걸로 설명된다.
**flash 는 아직 한 번 30초가 나왔다 — 완전히 정상은 아니다.**

### 무엇이 아닌지 (네 겹으로 배제)

- **우리 코드가 아니다.** 8/25 이후 커밋이 0건이다. 코드가 그대로다
- **입력 크기가 아니다.** 더 큰 입력이 10배 빨랐다
- **출력량이 아니다.** 생성량으로 정규화해도 7배가 남는다
- **네트워크·호스트가 아니다.** 로컬(재측정)과 배포(id 8)가 **동시에** 회복됐다

### 특정하지 못한 것 — 여기서 멈춘다

- 구글 공개 인시던트 기록에 8/24~8/26 해당 건이 **없다.** status.cloud.google.com
  의 8/20 건은 Vertex·us-west1 이라 우리 경로(AI Studio API 키)가 아니다
- **preview 백엔드 교체 가설은 검증 불가.** 응답의 `modelVersion` 이 별칭
  (`gemini-3.1-pro-preview`)을 그대로 되돌려 줘서 빌드가 바뀌었는지 알 수 없다
- 8/25 응답 원문을 `results/` 에 남기지 않아 대조할 것이 없다.
  **다음에 지연을 재면 원문도 같이 남긴다**

**"저절로 회복됐다"가 지금 시점의 정직한 최종 서술이다.** 8/24 에 대응을
미룬 판단은 결과적으로 옳았다. 8/25 에 넣은 경과 초 표시·30초 경고는
**재발할 때를 위해 그대로 둔다** — 지운 근거가 없다.

**정확도는 변동 없음.** 빨라진 것이지 답이 바뀐 게 아니다.

### 같은 날 함께 고친 것 — 세션 상한 (지연과 무관)

세션id 를 localStorage 에 두는데 `session_count` 에 날짜 조건이 없어
**한 브라우저가 평생 5회**였다. Streamlit 때는 `st.session_state` 라
새로고침만 해도 풀렸는데, FastAPI 로 옮기면서 의도한 적 없이 방어가 세졌다.
실제로 배포 DB 의 한 브라우저가 상한에 걸려 있었다(8/25 4건 + 8/27 1건).
하루 단위로 고쳤고, 배포에서 `세션남은` 0 → 4 로 확인했다.

---

## 2026-08-28 — IDOR 점검에서 시작해 상한 5건을 고쳤다 (정확도 무관)

"IDOR 를 시험한 적 있나"라는 물음에서 출발했다. **방어 코드는 있었지만
그게 실제로 막는지 확인한 적이 없었다**(커밋 `90264aa` 에 구현만 있음).

### IDOR — 이미 막고 있었다 (18건 통과)

저장 계층 8건 + HTTP 계층 10건. 남의 `run_id` 를 찍어도 `rowcount 0`,
인젝션 3종도 전부 0, `run_id` 1~20 순차 훑기로 고쳐진 행은 자기 것 1건뿐.
**없는 번호와 남의 번호의 응답이 바이트 단위로 같아** 번호 열거가 안 된다.
`세션id` 는 pydantic 필수 필드라 빼면 422 — 소유 확인이 없는 경로
(`save_feedback(세션id=None)`, Streamlit 용)로 우회할 수 없다.

### 그 과정에서 나온 것 — 상한이 경로마다 달랐다

비싼 쪽(분류, pro)은 **세션id 를 500번 갈아치워도 51회째에 막혔다.**
일일 카운터가 DB 라 위조에 견딘다. 문제는 나머지였다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| `POST /api/dropped` 새 세션 300회 | **300건 전부 통과** (74 req/s) | 50건에서 차단 |
| 그때 `runs` 행 / DB | 305건 / 4.7MB | 50건 / 0.80MB |
| `POST /api/catalog` 새 세션 | `상한_검사()` 자체를 안 부름 | 50건에서 차단, LLM 호출 0 |
| `POST /api/gate` 새 세션 500회 | **막힌 게 0건**, 카운터 0 그대로 | 150에서 차단 |
| 80MB 업로드 시 힙 | 80.2MB (다 읽고 400) | **1.5MB** (읽기 전 400) |
| 메모리 카운터 | 날짜가 바뀌어도 안 비워짐 | 자정에 비움 |

`/api/dropped` 가 제일 급했다. LLM 을 안 부르니 느려질 이유가 없어서
그 속도면 **Supabase 무료 500MB 가 6분에 찬다.** `runs` 표가 차면 봉인을
다 쓴 지금 유일하게 남은 측정 채널(👍/👎)이 같이 죽는다.

### 설계 판단 두 가지

**counters 표를 안 바꿨다.** 날짜 열이 TEXT 라 `'끊김:2026-08-28'` 을 키로
쓸 수 있다. 컬럼을 늘리면 `UPDATE counters SET {열} = ?` 이 되는데 **컬럼명은
바인딩이 안 돼** 문자열 조립이 필요하다 — 인젝션을 막아 놓고 표면을 여는 셈이다.
복합 키 정규화가 설계로는 맞지만 배포 Postgres 마이그레이션이 필요하고,
이 파일이 UPSERT 를 피한 것과 같은 방언 위험을 진다. 대신 `storage.py` 의
인자명을 `날짜` → `키` 로 바꿔 코드에서 의미를 맞췄다.

**업로드는 조각 읽기를 해 보고 되돌렸다.** 1MB 씩 읽어 모았다가 `b"".join`
하면 조각과 합친 것이 순간 같이 살아 있어 **9MB 업로드에서 9.1MB 가 18.1MB 로
늘었다.** 거절되는 큰 파일은 `UploadFile.size` 로 읽기 전에 막고, 통과한
것은 한 번에 읽는 쪽이 싸다. 통과 케이스는 고치기 전과 정확히 같다.

### 검증

LLM 호출부를 스텁으로 갈아 끼워 **실제 API 호출 0건**으로 쟀다.
①~⑤ + IDOR 통합 회귀 11/11 통과. 배포 URL·프로덕션 DB 는 건드리지 않았다.

**정확도는 변동 없음.** 방어를 고친 것이지 분류가 바뀐 게 아니다.
